# Scope Drift Log

One line per PR labeled `out-of-scope` during the refonte. Format:

```
YYYY-MM-DD | #PR | <author> | <one-line reason> | <approver>
```

If you are merging an `out-of-scope` PR, you must add a row here in the
same PR. CI checks for the presence of the new line.

The refonte fails its discipline target if this file grows by more than
**2 lines per week** during Phases 0 → 5.

---

<!-- entries below this line -->

| 2026-05-02 | #39 | feat(brand-portal) RAG sources + filtreur qualifiant | NEFER (batch merge) | Extension fonctionnelle post-Phase 15 — pas de nouveau Neter, pas de nouveau model Prisma, pas de bypass governance. ADR-0027 (renuméroté post-merge — collision 0023 avec OPERATOR_AMEND_PILLAR). Cf. Justification dans body PR. |
| 2026-05-07 | #79 | fix(governance) NEFER fine-review CI 3 erreurs + 3 anti-récidives | NEFER (auto) | Tooling CI + tests pré-existants (vault-matcher/resolver bugs depuis 0fe1407). Cap APOGEE 7/7 préservé, aucun Neter, aucun model Prisma. Anti-récidives = ESLint rule lafusee/no-vi-mock-toplevel-var + PR template + NEFER.md Phase 7.2/7.4. |
| 2026-05-07 | #80 | feat(artemis,seshat) market research console + structured ingest + NEFER §3 doctrine + Glory tool wrapper | NEFER (auto) | Étend ADR-0037 PR-I (Phase 17 country-scoped KB) avec voie manual-first parity ADR-0060 + corrections doctrinales rétroactives v6.20.1 : **(1)** template canonique `structured-market-study/v1` + parser déterministe ; **(2)** page Console `/console/seshat/market-research` avec recherche LLM-ancrée sources URL (anti-SSRF + anti-fab triple-couche) ; **(3)** export PDF jsPDF + persistance `KnowledgeEntry` cross-marques ; **(4)** **service déplacé `seshat/market-research/` → `artemis/market-research/`** (NEFER §3.2 : actions/séquences = Artemis governance) ; **(5)** **Glory tool wrapper `market-research-runner`** dans EXTENDED registry (NEFER §3.1 : Glory tools = primary API surface, pattern wrap-service-via-Intent identique Phase 14 Imhotep) ; **(6)** **doctrine NEFER §3 enrichie** : interdit #1 en 2 passes (Glory tools first + grep CODE-MAP), §3.1 pre-check Glory tools, §3.2 mapping Neter ↔ responsabilité. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **1 Intent kind** (`RUN_MARKET_RESEARCH`), **1 Glory tool EXTENDED** (cardinalité CORE 56 préservée). Web search natif tool-use bloqué par absence intégration LLM gateway. Embeddings RAG cross-brand reportés (résidu Phase 20). Promotion à GlorySequence first-class reportée (résidu Phase 21). Label `phase/17` n'existe pas dans le repo. |
| 2026-06-21 | #277 | feat(artemis) Glory tool `sales-response-tree` (arbre de réponse commercial) | NEFER (auto) — opérateur/fondateur | Fonctionnalité net-new de *sales enablement* hors phases 0–9 de la refonte. 1 Glory tool HYBRID dans EXTENDED (cardinalité CORE 56 préservée), invoqué via l'Intent existant `LEGACY_GLORY_EXECUTE` (aucun bypass). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma** (réutilise `CrmContact`/`Deal`/`DealStage` pour la capture lead). Parité manual-first ADR-0060 + sortie structurée ADR-0067. ADR-0104. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-21 | #278 | fix(governance) green two pre-existing vitest failures on main (writePillar allowlist line-drift + tarsis signal-type stale mock) | NEFER (auto) — opérateur | Dette de test pré-existante sur `main`, hors phases 0–9 (même nature que #79). **Test-only** : 0 code de production touché, 0 nouveau Neter, 0 model Prisma, 0 bypass. Greene la CI de `main`. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-21 | #280 | fix(ui) Golden Path 1-landing : lien mort `/legal/privacy` → `/privacy` (cause exacte) + icônes PWA manquantes + golden-path auto-diagnostiquable | NEFER (auto) — opérateur | Correctif racine du finding `1-landing` pré-existant (rouge depuis ~2026-05-31). Cause exacte révélée par le diagnostic ajouté (URL de la ressource) : le bandeau cookies pointait `<Link href="/legal/privacy">` (route inexistante) → Next prefetch → 404 RSC sur `/intake`. Corrigé en `/privacy`. Annexe : 2 icônes PWA manquantes référencées par `manifest.webmanifest` (générées depuis `src/app/icon.svg`). Hors phases 0–9 (même nature que #79/#278). Correctif d'un lien + 2 assets statiques + outillage CI, 0 nouveau Neter, 0 model Prisma, 0 bypass. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-22 | #288 | feat(marketing) vrai site d'agence multi-pages UPgraders en page d'accueil `/` (La Fusée → sous-site `/lafusee`) — 9 routes, distinction CTA agence/produit, contenu canon KB + dossier agence | NEFER (auto) — opérateur/fondateur | Fonctionnalité net-new de surface marketing publique (site vitrine de l'agence UPgraders) hors phases 0–9 de la refonte (même nature que #277/#80). **Pages de contenu statique** : 0 nouveau Neter (Cap APOGEE 7/7), 0 model Prisma, 0 bypass governance (aucun appel service/Intent — pages serveur de contenu). DS canon UPgraders respecté (3 gates anti-drift verts, tokens sémantiques uniquement). La Fusée préservée intacte sous `/lafusee` ; CTAs du funnel La Fusée (`/intake`) maintenus et distingués des CTAs commerciaux UPgraders (`/contact`). Contenu porté de la KB business + Folio_Spark (référence opérateur). tsc 0 · eslint 0 · build 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-22 | #289 | feat(crm,blog) formulaire contact UPgraders branché au CRM natif + CMS blog natif (modèle `Post`) + « Qui sommes-nous » + mentions légales UPgraders | NEFER (auto) — opérateur/fondateur | Suite du site UPgraders (#288), hors phases 0–9. **1 nouveau model Prisma `Post`** (additif, migration backfill-safe `20260622000000_blog_post_cms`) — pas un doublon (grep CODE-MAP négatif : ni `Article`/`BlogPost`/`Content` ; distinct de `BrandAsset`/`KnowledgeEntry`). **0 nouveau Neter** (Cap APOGEE 7/7), **0 bypass governance** : capture lead (`/api/contact` → `CrmContact`/`CrmMessage`) et CRUD blog (router `blog`) en direct-`db` éditorial, précédent établi `crm-contacts`/`/api/newsletter`. CRM déjà natif (pas de WordPress). Blog DB-first + fallback bundle. Éditeur Console `/console/anubis/blog`. tsc 0 · eslint 0 · 855 tests gouvernance verts · `prisma validate` ok · `next build` exit 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-22 | #290 | chore(deploy) seed blog automatisé au build Vercel (après `migrate deploy`, non-bloquant) + seed rendu create-only | NEFER (auto) — opérateur/fondateur | Suite de #289, hors phases 0–9. **0 schéma / 0 model / 0 Neter** (Cap APOGEE 7/7), 0 bypass : `vercel.json` buildCommand étendu + `prisma/seed-blog.ts` passé d'upsert à create-only (préserve les éditions Console). Le sandbox n'a pas de credential DB → câblage déploiement plutôt qu'exécution manuelle. tsc 0 · vercel.json valide. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #298 | feat(site-prober) bot testeur black-box de production (crawler/prober read-only) + premier sondage de `lafusee-app.vercel.app` | NEFER (auto) — opérateur/fondateur | Outil de QA/test externe net-new, hors phases 0–9 de la refonte (même nature que #79/#280 : tooling/CI). **Zéro couplage `src/**`** — parle uniquement HTTP, tourne même si l'app ne compile pas ; vit sous `scripts/site-prober/`, déjà exclu de `tsconfig` et du lint `src/**`, donc n'affecte aucun gate de production. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance** (n'appelle aucun service ni Intent — testeur black-box read-only GET/HEAD, jamais de POST/webhook/paiement). Sortie écrite dans `reports/` (gitignored). Le premier sondage a révélé 9 CRITICAL (les endpoints `/api/cron/*` s'exécutent sans authentification). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #299 | fix(security) endpoints `/api/cron/*` fail-closed (helper partagé `cron-auth`) + harnais `/api/test-e2e` gardé | NEFER (auto) — opérateur/fondateur | Hotfix de sécurité hors phases 0–9 (même nature que #79/#280) : les 10 routes cron s'exécutaient sans auth en prod (`if (!cronSecret) return true` fail-open + `CRON_SECRET` non défini) et `test-e2e` écrivait en base anonymement. Découvert par le bot `site-prober` (#298). Nouveau helper `src/lib/cron-auth.ts` fail-CLOSED en prod ; 9 copies locales supprimées + garde ajoutée à `feedback-loop` ; `test-e2e` → 404 en prod sans bearer. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (ajoute une garde, ne touche ni service ni Intent ni schéma). Action déploiement : définir `CRON_SECRET` (Vercel env + secret GitHub Actions). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #300 | fix(security) en-têtes globaux + MCP catalogue gardé + admin-metrics fail-closed (Lot 1 plan remédiation site-prober) | NEFER (auto) — opérateur/fondateur | Hotfix sécurité hors phases 0–9 (même nature que #299). En-têtes globaux `next.config.ts` (HSTS/nosniff/frame/referrer/permissions enforced + CSP Report-Only) + `poweredByHeader:false` ; `GET` catalogue MCP des 9 serveurs gardé par `authenticateMcpRequest` (POST déjà gardé) ; `/api/admin/metrics` → 404 au lieu de 500 fuitant le nom de var. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (config + gardes d'auth). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #301 | fix(ui) fil d'Ariane (liens morts) + `/launchpad/*` protégé + robots/sitemap (Lot 2+3 plan remédiation site-prober) | NEFER (auto) — opérateur/fondateur | Correctifs UX+SEO hors phases 0–9 (même nature que #280/#300). Breadcrumb ne linke que les routes navigables (manifeste généré `src/lib/generated/app-routes.ts`) → fin des 15 liens morts vers racines de section sans page ; `/launchpad/*` (assistants opérateur) ajouté aux `PROTECTED_ROUTES` + matcher `proxy.ts` ; `app/robots.ts` + `app/sitemap.ts` natifs. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #302 | perf(ui) images lourdes recompressées + assets morts purgés (Lot 4 plan remédiation site-prober) | NEFER (auto) — opérateur/fondateur | Optimisation perf hors phases 0–9 (même nature que #300/#301). `public/brand` 66→6,3 Mo : 41 Mo d'assets non-référencés purgés + 3 photos d'accueil → WebP (resize 1280px) + portraits redimensionnés. 4 refs d'images mises à jour, `loading=lazy` + width/height anti-CLS. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (assets statiques). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #303 | feat(tooling) auditeur de sécurité des nœuds LLM (entrée + sortie) | NEFER (auto) — opérateur/fondateur | Outil QA/sécurité net-new hors phases 0–9 (même nature que #298 site-prober : tooling). `scripts/audit-llm-nodes.ts` introspecte le registre (validation sortie : Glory 21/76, frameworks 28/28) + scanne les appels LLM directs (52/38 fichiers) ; rapport `docs/governance/llm-node-audit.md` + baseline régression-only en `--strict`. Vit sous `scripts/` (exclu des gates `src/**`). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance** (lecture/analyse seule). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #305 | feat(meta) sécurité LLM LOT 0 — verrou d'entrée anti-injection | NEFER (auto) — opérateur/fondateur | Hotfix sécurité hors phases 0–9 (LOT 0 du plan de durcissement validé opérateur, `docs/governance/llm-hardening-plan.md`). Utilitaire `untrusted-content` (neutralisation + balisage anti-injection) branché à 3 chokepoints : wrapper structuré (tout nœud), `engine.ts` (outils), `artemis/index.ts` (frameworks) ; rappel sécurité dans le system prompt ; 7 tests. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (durcissement entrée, ne touche ni service ni Intent ni schéma). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #306 | feat(intake) sécurité LLM LOT 1a — durcissement des points d'intake | NEFER (auto) — opérateur/fondateur | Hotfix sécurité hors phases 0–9 (LOT 1a du plan, `docs/governance/llm-hardening-plan.md`). Les appels LLM directs des points d'intake contournent les chokepoints LOT 0 → durcis en entrée ET sortie : `deduce-adve` (entrées neutralisées + fence canonique ; sortie déjà Zod) + `boot-sequence` (contenu existant balisé + sortie Zod par question au lieu de JSON.parse brut). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #308 | feat(intake) sécurité LLM LOT 1a (suite) — narrate-adve + rtis-draft (entrée) | NEFER (auto) — opérateur/fondateur | Suite LOT 1a du plan. Durcissement entrée des 2 derniers appels LLM directs du cœur quick-intake : `narrate-adve` (valeurs verbatim founder + brandName neutralisés sans altérer le verbatim) + `rtis-draft` (companyName neutralisé + contexte marque ADVE/RAG fencé). Sorties déjà throw-on-invalid ; Zod complet = follow-up. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #309 | feat(intake) sécurité LLM LOT 1a (fin) — brief-ingest (entrée) | NEFER (auto) — opérateur/fondateur | Clôture LOT 1a. `brief-ingest/analyzer` : `rawText` du brief (document client) fencé via `wrapUntrusted` (neutralise le breakout `=== FIN DU BRIEF ===`) ; sortie déjà Zod (`parsedBriefSchema.safeParse`). OCR vision sans vecteur. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #310 | feat(meta) sécurité LLM LOT 1b — durcissement entrée services de dérivation | NEFER (auto) — opérateur/fondateur | LOT 1b du plan. `rtis-cascade` + `notoria` + `mestor/insights` (appels LLM directs hors chokepoints LOT 0) : contenu pilier balisé via `wrapUntrusted` (dans `serializePillar` → toutes branches R/T/I/S, et `contextLines`) + `UNTRUSTED_NOTICE` au system. Entrée durcie ; sortie multi-formes = follow-up Zod. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 46 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #311 | ci(meta) sécurité LLM LOT 2 — gate CI anti-régression | NEFER (auto) — opérateur/fondateur | LOT 2 du plan. Job `LLM node guardrails (no regression)` dans `ci.yml` → `npm run audit:llm:strict` à chaque PR (échoue sur nouveau nœud non protégé vs baseline, sans imposer le backlog). Verrouille l'acquis sécurité (injection fermée LOT 0/1a/1b). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance** (tooling CI). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #312 | fix(test) numéro de ligne allowlist C5 (boot-sequence 191→210) — main au vert | NEFER (auto) — opérateur/fondateur | Hotfix CI : #306 (LOT 1a) a décalé de ~19 lignes l'écriture `Pillar.content` legacy-normalize de boot-sequence ; l'allowlist line-number du test KEYSTONE C5 pointait encore 191 → 2 échecs vitest. Numéro corrigé 191→210, suite vitest `main` au vert. **0 code prod** (test-only). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #313 | feat(artemis) sécurité LLM LOT 1c (batch 1) — schémas de sortie réels 7 outils CR | NEFER (auto) — opérateur/fondateur | LOT 1c du plan. Vrais schémas de sortie (PAS de permissif attrape-tout) dérivés du `promptTemplate` de 7 outils CR (`glory-output-schemas.ts` feuille zod-only). `executeStructuredLLMCall` réinjecte + valide + retry. Baseline gate ratchetée 37→30. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 97 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #314 | feat(artemis) sécurité LLM LOT 1c (batch 2) — schémas de sortie réels 4 outils DC | NEFER (auto) — opérateur/fondateur | Suite LOT 1c. Vrais schémas dérivés des prompts (3 contrats JSON explicites) : `coherence-checker`, `brand-guardian` (audit verrouillé + verdict enum), `insight-synthesizer`, `idea-killer-saver`. Baseline gate ratchetée 30→26. `synthesize-section` tenu à l'écart (chemin Oracle). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 40 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #315 | feat(artemis) sécurité LLM LOT 1c (batch 3) — schémas de sortie réels 4 outils BRAND | NEFER (auto) — opérateur/fondateur | Suite LOT 1c. Vrais schémas pour 4 outils BRAND SANS forgeOutput couplé : `semiotic-brand-analyzer`, `logo-type-advisor`, `logo-validation-protocol`, `motion-identity-designer`. Forge-couplés tenus à l'écart (briefTextPath, vérif fonctionnelle requise). Baseline gate ratchetée 26→22 ; 15/37 outils validés. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #316 | feat(artemis) sécurité LLM LOT 1c (batch 4) — schémas de sortie réels 5 outils CR | NEFER (auto) — opérateur/fondateur | Suite LOT 1c. Vrais schémas pour 5 outils CR copywriting : `tone-of-voice-designer`, `manifesto-writer`, `engagement-rituals-designer`, `claim-architect`, `vocabulary-builder`. Baseline gate ratchetée 22→17 ; 20/37 outils validés. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 40 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #317 | feat(artemis) sécurité LLM LOT 1c (batch 5) — schémas de sortie réels 17 derniers outils → 37/37 | NEFER (auto) — opérateur/fondateur | Clôture LOT 1c. Vrais schémas dérivés des prompts pour les 17 outils restants : 7 registry (visual-moodboard, synthesize-section, 5× lsi), 3 Imhotep (crew-matcher, formation-recommender, qc-evaluator), 2 Anubis (ad-copy-generator, audience-targeter), 4 AD/OPS (expand/cross/decode/defend), 1 postmortem-12q. Champs couplés-forge préservés. Baseline gate ratchetée 17→0 ; **37/37 outils + 76/76 nœuds LLM/HYBRID + 28/28 frameworks à 100 %**. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #318 | feat(meta) sécurité LLM LOT 1e — verrou d'entrée anti-injection sur tous les appels directs → 37/37 | NEFER (auto) — opérateur/fondateur | Clôture LOT 1 du plan. Entrée (anti-injection OWASP LLM01) neutralisée sur les 28 fichiers d'appel LLM direct restants (quick-intake, rtis-protocols, seshat, mestor/pillar-maturity, ingestion/sources, générateurs/oracle) via `wrapUntrusted`/`sanitizeInline` + `UNTRUSTED_NOTICE`. Auditeur renforcé (verdict `FENCED`/`INTERNAL`/`RAW` par fichier + faux positif chaîne `description` corrigé) ; gate `--strict` bloque toute nouvelle entrée brute. **37/37 fichiers à entrée durcie, 0 brut** ; sortie maintenue 76/76 + 28/28. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (durcissement entrée + tooling audit + 1 ligne allowlist test C5 `strategy.ts` 713→722). tsc 0 · eslint 0 · 2145 tests verts. Cf. Justification — out-of-scope dans le body PR. |
