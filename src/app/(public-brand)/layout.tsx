/**
 * Layout du groupe public-brand — pages de marque par sous-domaine.
 * Léger : pas de shell portail, la page porte son propre style (pb-*).
 */
import "@/styles/globals.css";

export default function PublicBrandLayout({ children }: { children: React.ReactNode }) {
  return children;
}
