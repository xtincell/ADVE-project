# Scope Drift Log

One line per PR labeled `out-of-scope` during the refonte. Format:

```
YYYY-MM-DD | #PR | <author> | <one-line reason> | <approver>
```

If you are merging an `out-of-scope` PR, you must add a row here in the
same PR. CI checks for the presence of the new line.

The refonte fails its discipline target if this file grows by more than
**2 lines per week** during Phases 0 → 5.

---

<!-- entries below this line -->

| 2026-05-02 | #39 | feat(brand-portal) RAG sources + filtreur qualifiant | NEFER (batch merge) | Extension fonctionnelle post-Phase 15 — pas de nouveau Neter, pas de nouveau model Prisma, pas de bypass governance. ADR-0027 (renuméroté post-merge — collision 0023 avec OPERATOR_AMEND_PILLAR). Cf. Justification dans body PR. |
| 2026-05-07 | #79 | fix(governance) NEFER fine-review CI 3 erreurs + 3 anti-récidives | NEFER (auto) | Tooling CI + tests pré-existants (vault-matcher/resolver bugs depuis 0fe1407). Cap APOGEE 7/7 préservé, aucun Neter, aucun model Prisma. Anti-récidives = ESLint rule lafusee/no-vi-mock-toplevel-var + PR template + NEFER.md Phase 7.2/7.4. |
| 2026-05-07 | #80 | feat(artemis,seshat) market research console + structured ingest + NEFER §3 doctrine + Glory tool wrapper | NEFER (auto) | Étend ADR-0037 PR-I (Phase 17 country-scoped KB) avec voie manual-first parity ADR-0060 + corrections doctrinales rétroactives v6.20.1 : **(1)** template canonique `structured-market-study/v1` + parser déterministe ; **(2)** page Console `/console/seshat/market-research` avec recherche LLM-ancrée sources URL (anti-SSRF + anti-fab triple-couche) ; **(3)** export PDF jsPDF + persistance `KnowledgeEntry` cross-marques ; **(4)** **service déplacé `seshat/market-research/` → `artemis/market-research/`** (NEFER §3.2 : actions/séquences = Artemis governance) ; **(5)** **Glory tool wrapper `market-research-runner`** dans EXTENDED registry (NEFER §3.1 : Glory tools = primary API surface, pattern wrap-service-via-Intent identique Phase 14 Imhotep) ; **(6)** **doctrine NEFER §3 enrichie** : interdit #1 en 2 passes (Glory tools first + grep CODE-MAP), §3.1 pre-check Glory tools, §3.2 mapping Neter ↔ responsabilité. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **1 Intent kind** (`RUN_MARKET_RESEARCH`), **1 Glory tool EXTENDED** (cardinalité CORE 56 préservée). Web search natif tool-use bloqué par absence intégration LLM gateway. Embeddings RAG cross-brand reportés (résidu Phase 20). Promotion à GlorySequence first-class reportée (résidu Phase 21). Label `phase/17` n'existe pas dans le repo. |
| 2026-06-21 | #277 | feat(artemis) Glory tool `sales-response-tree` (arbre de réponse commercial) | NEFER (auto) — opérateur/fondateur | Fonctionnalité net-new de *sales enablement* hors phases 0–9 de la refonte. 1 Glory tool HYBRID dans EXTENDED (cardinalité CORE 56 préservée), invoqué via l'Intent existant `LEGACY_GLORY_EXECUTE` (aucun bypass). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma** (réutilise `CrmContact`/`Deal`/`DealStage` pour la capture lead). Parité manual-first ADR-0060 + sortie structurée ADR-0067. ADR-0104. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-21 | #278 | fix(governance) green two pre-existing vitest failures on main (writePillar allowlist line-drift + tarsis signal-type stale mock) | NEFER (auto) — opérateur | Dette de test pré-existante sur `main`, hors phases 0–9 (même nature que #79). **Test-only** : 0 code de production touché, 0 nouveau Neter, 0 model Prisma, 0 bypass. Greene la CI de `main`. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-21 | #280 | fix(ui) Golden Path 1-landing : lien mort `/legal/privacy` → `/privacy` (cause exacte) + icônes PWA manquantes + golden-path auto-diagnostiquable | NEFER (auto) — opérateur | Correctif racine du finding `1-landing` pré-existant (rouge depuis ~2026-05-31). Cause exacte révélée par le diagnostic ajouté (URL de la ressource) : le bandeau cookies pointait `<Link href="/legal/privacy">` (route inexistante) → Next prefetch → 404 RSC sur `/intake`. Corrigé en `/privacy`. Annexe : 2 icônes PWA manquantes référencées par `manifest.webmanifest` (générées depuis `src/app/icon.svg`). Hors phases 0–9 (même nature que #79/#278). Correctif d'un lien + 2 assets statiques + outillage CI, 0 nouveau Neter, 0 model Prisma, 0 bypass. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-22 | #288 | feat(marketing) vrai site d'agence multi-pages UPgraders en page d'accueil `/` (La Fusée → sous-site `/lafusee`) — 9 routes, distinction CTA agence/produit, contenu canon KB + dossier agence | NEFER (auto) — opérateur/fondateur | Fonctionnalité net-new de surface marketing publique (site vitrine de l'agence UPgraders) hors phases 0–9 de la refonte (même nature que #277/#80). **Pages de contenu statique** : 0 nouveau Neter (Cap APOGEE 7/7), 0 model Prisma, 0 bypass governance (aucun appel service/Intent — pages serveur de contenu). DS canon UPgraders respecté (3 gates anti-drift verts, tokens sémantiques uniquement). La Fusée préservée intacte sous `/lafusee` ; CTAs du funnel La Fusée (`/intake`) maintenus et distingués des CTAs commerciaux UPgraders (`/contact`). Contenu porté de la KB business + Folio_Spark (référence opérateur). tsc 0 · eslint 0 · build 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-22 | #289 | feat(crm,blog) formulaire contact UPgraders branché au CRM natif + CMS blog natif (modèle `Post`) + « Qui sommes-nous » + mentions légales UPgraders | NEFER (auto) — opérateur/fondateur | Suite du site UPgraders (#288), hors phases 0–9. **1 nouveau model Prisma `Post`** (additif, migration backfill-safe `20260622000000_blog_post_cms`) — pas un doublon (grep CODE-MAP négatif : ni `Article`/`BlogPost`/`Content` ; distinct de `BrandAsset`/`KnowledgeEntry`). **0 nouveau Neter** (Cap APOGEE 7/7), **0 bypass governance** : capture lead (`/api/contact` → `CrmContact`/`CrmMessage`) et CRUD blog (router `blog`) en direct-`db` éditorial, précédent établi `crm-contacts`/`/api/newsletter`. CRM déjà natif (pas de WordPress). Blog DB-first + fallback bundle. Éditeur Console `/console/anubis/blog`. tsc 0 · eslint 0 · 855 tests gouvernance verts · `prisma validate` ok · `next build` exit 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-22 | #290 | chore(deploy) seed blog automatisé au build Vercel (après `migrate deploy`, non-bloquant) + seed rendu create-only | NEFER (auto) — opérateur/fondateur | Suite de #289, hors phases 0–9. **0 schéma / 0 model / 0 Neter** (Cap APOGEE 7/7), 0 bypass : `vercel.json` buildCommand étendu + `prisma/seed-blog.ts` passé d'upsert à create-only (préserve les éditions Console). Le sandbox n'a pas de credential DB → câblage déploiement plutôt qu'exécution manuelle. tsc 0 · vercel.json valide. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #298 | feat(site-prober) bot testeur black-box de production (crawler/prober read-only) + premier sondage de `lafusee-app.vercel.app` | NEFER (auto) — opérateur/fondateur | Outil de QA/test externe net-new, hors phases 0–9 de la refonte (même nature que #79/#280 : tooling/CI). **Zéro couplage `src/**`** — parle uniquement HTTP, tourne même si l'app ne compile pas ; vit sous `scripts/site-prober/`, déjà exclu de `tsconfig` et du lint `src/**`, donc n'affecte aucun gate de production. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance** (n'appelle aucun service ni Intent — testeur black-box read-only GET/HEAD, jamais de POST/webhook/paiement). Sortie écrite dans `reports/` (gitignored). Le premier sondage a révélé 9 CRITICAL (les endpoints `/api/cron/*` s'exécutent sans authentification). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #299 | fix(security) endpoints `/api/cron/*` fail-closed (helper partagé `cron-auth`) + harnais `/api/test-e2e` gardé | NEFER (auto) — opérateur/fondateur | Hotfix de sécurité hors phases 0–9 (même nature que #79/#280) : les 10 routes cron s'exécutaient sans auth en prod (`if (!cronSecret) return true` fail-open + `CRON_SECRET` non défini) et `test-e2e` écrivait en base anonymement. Découvert par le bot `site-prober` (#298). Nouveau helper `src/lib/cron-auth.ts` fail-CLOSED en prod ; 9 copies locales supprimées + garde ajoutée à `feedback-loop` ; `test-e2e` → 404 en prod sans bearer. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (ajoute une garde, ne touche ni service ni Intent ni schéma). Action déploiement : définir `CRON_SECRET` (Vercel env + secret GitHub Actions). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #300 | fix(security) en-têtes globaux + MCP catalogue gardé + admin-metrics fail-closed (Lot 1 plan remédiation site-prober) | NEFER (auto) — opérateur/fondateur | Hotfix sécurité hors phases 0–9 (même nature que #299). En-têtes globaux `next.config.ts` (HSTS/nosniff/frame/referrer/permissions enforced + CSP Report-Only) + `poweredByHeader:false` ; `GET` catalogue MCP des 9 serveurs gardé par `authenticateMcpRequest` (POST déjà gardé) ; `/api/admin/metrics` → 404 au lieu de 500 fuitant le nom de var. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (config + gardes d'auth). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #301 | fix(ui) fil d'Ariane (liens morts) + `/launchpad/*` protégé + robots/sitemap (Lot 2+3 plan remédiation site-prober) | NEFER (auto) — opérateur/fondateur | Correctifs UX+SEO hors phases 0–9 (même nature que #280/#300). Breadcrumb ne linke que les routes navigables (manifeste généré `src/lib/generated/app-routes.ts`) → fin des 15 liens morts vers racines de section sans page ; `/launchpad/*` (assistants opérateur) ajouté aux `PROTECTED_ROUTES` + matcher `proxy.ts` ; `app/robots.ts` + `app/sitemap.ts` natifs. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #302 | perf(ui) images lourdes recompressées + assets morts purgés (Lot 4 plan remédiation site-prober) | NEFER (auto) — opérateur/fondateur | Optimisation perf hors phases 0–9 (même nature que #300/#301). `public/brand` 66→6,3 Mo : 41 Mo d'assets non-référencés purgés + 3 photos d'accueil → WebP (resize 1280px) + portraits redimensionnés. 4 refs d'images mises à jour, `loading=lazy` + width/height anti-CLS. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (assets statiques). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #307 | feat(pricing) séparation univers tarifaire UPgraders ≠ La Fusée + catalogue gamme réelle (9 offres) ; feat(wakanda) scanner de couverture + plan « LA TOTALE » ; feat(governance) market kill-switch marché (ADR-0105) | NEFER (auto) — opérateur/fondateur | Surface marketing/funnel publique (pages tarifs : séparation des univers UPgraders/La Fusée, `<ProductCatalog>` + `PRODUCTS`/`PRODUCT_CATEGORIES`) + outillage seed/QA (`scripts/seed-wakanda/coverage-scan.ts` + `docs/scan/wakanda-coverage-plan.md`), hors phases 0–9 (même nature que #288/#298). **0 nouveau Neter** (Cap APOGEE 7/7), **0 bypass**. Pricing/wakanda : pages serveur + script standalone exclu du lint `src/**`, DS canon respecté (3 gates anti-drift verts). **Market kill-switch (ADR-0105)** : cycle de vie marché sur `Country` (enum `MarketStatus` + 4 colonnes additives, migration `20260623120000` — **pas un nouveau model**), filtre-lecture default-deny `marketScopedDb` câblé dans le contexte tRPC (durcissement de visibilité ADMIN-bypass, **pas un bypass governance**), résolveur de visibilité par ensembles d'IDs descendants, 3 Intents gouvernés via `emitIntent` (à suivre). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #303 | feat(tooling) auditeur de sécurité des nœuds LLM (entrée + sortie) | NEFER (auto) — opérateur/fondateur | Outil QA/sécurité net-new hors phases 0–9 (même nature que #298 site-prober : tooling). `scripts/audit-llm-nodes.ts` introspecte le registre (validation sortie : Glory 21/76, frameworks 28/28) + scanne les appels LLM directs (52/38 fichiers) ; rapport `docs/governance/llm-node-audit.md` + baseline régression-only en `--strict`. Vit sous `scripts/` (exclu des gates `src/**`). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance** (lecture/analyse seule). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #305 | feat(meta) sécurité LLM LOT 0 — verrou d'entrée anti-injection | NEFER (auto) — opérateur/fondateur | Hotfix sécurité hors phases 0–9 (LOT 0 du plan de durcissement validé opérateur, `docs/governance/llm-hardening-plan.md`). Utilitaire `untrusted-content` (neutralisation + balisage anti-injection) branché à 3 chokepoints : wrapper structuré (tout nœud), `engine.ts` (outils), `artemis/index.ts` (frameworks) ; rappel sécurité dans le system prompt ; 7 tests. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (durcissement entrée, ne touche ni service ni Intent ni schéma). tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #306 | feat(intake) sécurité LLM LOT 1a — durcissement des points d'intake | NEFER (auto) — opérateur/fondateur | Hotfix sécurité hors phases 0–9 (LOT 1a du plan, `docs/governance/llm-hardening-plan.md`). Les appels LLM directs des points d'intake contournent les chokepoints LOT 0 → durcis en entrée ET sortie : `deduce-adve` (entrées neutralisées + fence canonique ; sortie déjà Zod) + `boot-sequence` (contenu existant balisé + sortie Zod par question au lieu de JSON.parse brut). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #308 | feat(intake) sécurité LLM LOT 1a (suite) — narrate-adve + rtis-draft (entrée) | NEFER (auto) — opérateur/fondateur | Suite LOT 1a du plan. Durcissement entrée des 2 derniers appels LLM directs du cœur quick-intake : `narrate-adve` (valeurs verbatim founder + brandName neutralisés sans altérer le verbatim) + `rtis-draft` (companyName neutralisé + contexte marque ADVE/RAG fencé). Sorties déjà throw-on-invalid ; Zod complet = follow-up. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #309 | feat(intake) sécurité LLM LOT 1a (fin) — brief-ingest (entrée) | NEFER (auto) — opérateur/fondateur | Clôture LOT 1a. `brief-ingest/analyzer` : `rawText` du brief (document client) fencé via `wrapUntrusted` (neutralise le breakout `=== FIN DU BRIEF ===`) ; sortie déjà Zod (`parsedBriefSchema.safeParse`). OCR vision sans vecteur. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #310 | feat(meta) sécurité LLM LOT 1b — durcissement entrée services de dérivation | NEFER (auto) — opérateur/fondateur | LOT 1b du plan. `rtis-cascade` + `notoria` + `mestor/insights` (appels LLM directs hors chokepoints LOT 0) : contenu pilier balisé via `wrapUntrusted` (dans `serializePillar` → toutes branches R/T/I/S, et `contextLines`) + `UNTRUSTED_NOTICE` au system. Entrée durcie ; sortie multi-formes = follow-up Zod. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 46 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #311 | ci(meta) sécurité LLM LOT 2 — gate CI anti-régression | NEFER (auto) — opérateur/fondateur | LOT 2 du plan. Job `LLM node guardrails (no regression)` dans `ci.yml` → `npm run audit:llm:strict` à chaque PR (échoue sur nouveau nœud non protégé vs baseline, sans imposer le backlog). Verrouille l'acquis sécurité (injection fermée LOT 0/1a/1b). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance** (tooling CI). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #312 | fix(test) numéro de ligne allowlist C5 (boot-sequence 191→210) — main au vert | NEFER (auto) — opérateur/fondateur | Hotfix CI : #306 (LOT 1a) a décalé de ~19 lignes l'écriture `Pillar.content` legacy-normalize de boot-sequence ; l'allowlist line-number du test KEYSTONE C5 pointait encore 191 → 2 échecs vitest. Numéro corrigé 191→210, suite vitest `main` au vert. **0 code prod** (test-only). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #313 | feat(artemis) sécurité LLM LOT 1c (batch 1) — schémas de sortie réels 7 outils CR | NEFER (auto) — opérateur/fondateur | LOT 1c du plan. Vrais schémas de sortie (PAS de permissif attrape-tout) dérivés du `promptTemplate` de 7 outils CR (`glory-output-schemas.ts` feuille zod-only). `executeStructuredLLMCall` réinjecte + valide + retry. Baseline gate ratchetée 37→30. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 97 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #314 | feat(artemis) sécurité LLM LOT 1c (batch 2) — schémas de sortie réels 4 outils DC | NEFER (auto) — opérateur/fondateur | Suite LOT 1c. Vrais schémas dérivés des prompts (3 contrats JSON explicites) : `coherence-checker`, `brand-guardian` (audit verrouillé + verdict enum), `insight-synthesizer`, `idea-killer-saver`. Baseline gate ratchetée 30→26. `synthesize-section` tenu à l'écart (chemin Oracle). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 40 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #315 | feat(artemis) sécurité LLM LOT 1c (batch 3) — schémas de sortie réels 4 outils BRAND | NEFER (auto) — opérateur/fondateur | Suite LOT 1c. Vrais schémas pour 4 outils BRAND SANS forgeOutput couplé : `semiotic-brand-analyzer`, `logo-type-advisor`, `logo-validation-protocol`, `motion-identity-designer`. Forge-couplés tenus à l'écart (briefTextPath, vérif fonctionnelle requise). Baseline gate ratchetée 26→22 ; 15/37 outils validés. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #316 | feat(artemis) sécurité LLM LOT 1c (batch 4) — schémas de sortie réels 5 outils CR | NEFER (auto) — opérateur/fondateur | Suite LOT 1c. Vrais schémas pour 5 outils CR copywriting : `tone-of-voice-designer`, `manifesto-writer`, `engagement-rituals-designer`, `claim-architect`, `vocabulary-builder`. Baseline gate ratchetée 22→17 ; 20/37 outils validés. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · 40 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #317 | feat(artemis) sécurité LLM LOT 1c (batch 5) — schémas de sortie réels 17 derniers outils → 37/37 | NEFER (auto) — opérateur/fondateur | Clôture LOT 1c. Vrais schémas dérivés des prompts pour les 17 outils restants : 7 registry (visual-moodboard, synthesize-section, 5× lsi), 3 Imhotep (crew-matcher, formation-recommender, qc-evaluator), 2 Anubis (ad-copy-generator, audience-targeter), 4 AD/OPS (expand/cross/decode/defend), 1 postmortem-12q. Champs couplés-forge préservés. Baseline gate ratchetée 17→0 ; **37/37 outils + 76/76 nœuds LLM/HYBRID + 28/28 frameworks à 100 %**. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. tsc 0 · eslint 0 · tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-23 | #318 | feat(meta) sécurité LLM LOT 1e — verrou d'entrée anti-injection sur tous les appels directs → 37/37 | NEFER (auto) — opérateur/fondateur | Clôture LOT 1 du plan. Entrée (anti-injection OWASP LLM01) neutralisée sur les 28 fichiers d'appel LLM direct restants (quick-intake, rtis-protocols, seshat, mestor/pillar-maturity, ingestion/sources, générateurs/oracle) via `wrapUntrusted`/`sanitizeInline` + `UNTRUSTED_NOTICE`. Auditeur renforcé (verdict `FENCED`/`INTERNAL`/`RAW` par fichier + faux positif chaîne `description` corrigé) ; gate `--strict` bloque toute nouvelle entrée brute. **37/37 fichiers à entrée durcie, 0 brut** ; sortie maintenue 76/76 + 28/28. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (durcissement entrée + tooling audit + 1 ligne allowlist test C5 `strategy.ts` 713→722). tsc 0 · eslint 0 · 2145 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-24 | #320 | ci(meta) workflow manuel `seed/scan/purge` Wakanda (`workflow_dispatch`) | NEFER (auto) — opérateur/fondateur | Outillage CI/ops hors phases 0–9 (même nature que #303/#311 tooling/CI). `.github/workflows/seed-wakanda.yml` exécute les scripts seed **existants** (`scripts/seed-wakanda/`, mergés #307) contre le secret `DATABASE_URL` d'un Environnement GitHub ; garde-fous : confirmation `WAKANDA` (sauf `scan`) + `DATABASE_URL` présent + `prisma migrate deploy` optionnel + `concurrency` par env. Données de démo réversibles (`isDummy=true` → tâche `purge`). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance**. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-24 | #321 | fix(ui) polish design grille `/pricing` (tokens sémantiques + skeleton) | NEFER (auto) — opérateur/fondateur | Polish d'une surface marketing/funnel publique (`/pricing`), hors phases 0–9 (même nature que #307 pages tarifs). Cartes non-featured `border bg-white` → `border border-border bg-background` (le `border` nu Tailwind v4 retombe sur currentColor ≈ noir sur thème bone → bordure lourde + carte stark, incohérente avec les cartes jumelles de `/tarifs`) ; skeleton `md:grid-cols-3` → `md:grid-cols-2 lg:grid-cols-3` (saut de layout). Purement visuel, aucun changement de contenu/prix. DS anti-drift 5/5 verts, eslint clean. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance**. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-28 | #357 | fix passe de debug NEFER — 4 couacs runtime (getCampaignTimeline 500, mutations muettes, coûts prod, 404 créateur) | NEFER (auto) — opérateur | Correctifs de bugs runtime production hors phases 0–9 (même nature que #79/#278/#280). CI verte ⇒ couacs runtime que la CI ne voit pas. 4 fix : `getCampaignTimeline` sélectionnait 5 clés `select` Prisma inexistantes (500 garanti, endpoint latent) ; `MutationCache.onError` global + `ToastProvider` monté globalement (mutations muettes « bouton inerte », surfaces argent) ; `ensureActionCostCatalog` auto-amorçage si table vide (parité prod, classe ADR-0119) ; CTA promotion créateur 404 → `/creator/messages`. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (extension du seed existant, filet UI, repoint de lien ; bus toast en couche `lib` = cascade respectée). eslint 0 · madge 0 cycle ; tsc/tests délégués à la CI (engine Prisma bloqué en local). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-28 | #358 | fix passe de debug NEFER (items différés) — badge saturation, boot complete, cadence H2 | NEFER (auto) — opérateur | Suite de la passe de debug runtime (#357), hors phases 0–9. 3 fix : badge UI `degradationCodes` saturation (la donnée exposait déjà `MVP_PLACEHOLDER_CAPACITY_LIMITS`) ; `boot/[sessionId]` appelle enfin `complete` à la dernière étape (`status` ACTIVE) ; cadence éditoriale flaggée `rythmeSource=DEFAULT_TEMPLATE` (honnêteté trou H2). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass** (1 champ additif optionnel sur type/Zod existant). eslint 0 · 3 tests DS verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #359 | fix(governance)+chore(deps) passe de debug NEFER — 13 manifests Phase 24 + purge 5 CVE high (wrangler retiré, xlsx→exceljs) | NEFER (auto) — opérateur/fondateur | Passe de debug/hygiène hors phases 0–9 (même nature que #298–302 site-prober / #313–316 LLM lots). **(1)** 13 services Phase 24 sans `manifest.ts` (drift `audit:governance` Phase 2) reçoivent leur manifeste — gouverneur dérivé de la source autoritaire `intent-kinds.ts` ; + fix `auto-promotion` (exportait `autoPromotionManifest` → absent du registry, `manifests:audit` plantait) + `campaign-tracker` (dépendance `thot` inexistante → `financial-brain`). **(2)** `wrangler` retiré (devDep + scripts `cf:*`) → 5 CVE **high** transitives éliminées (esbuild/miniflare/undici/ws) ; CF déployable via `npx`, Vercel canonique. **(3)** `xlsx` (SheetJS, 2 CVE high sans correctif npm, parseur d'uploads) → `exceljs` via helper `xlsx-read` (3 sites migrés). **(4)** script `typecheck` + lint cross-platform (`eslint src`). `npm audit` 12→8 (**0 high**, 0 low). **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance**. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #363 | docs(governance) interdit `npm audit fix --force` (incident 2026-06-29) | NEFER (auto) — opérateur/fondateur | Note de doctrine governance hors phases 0–9 (suite #359). `npm audit fix --force` (lancé pour 8 moderates) a downgradé next 16→9 / prisma 7→6 / exceljs 4→3 (footgun SemVer-major) → audit 8 moderate → **94 vulns (1 critical loader-utils, 26 high)** + build cassé (Next 9 = ni app-router ni RSC). Dégât working-tree (non committé) annulé via `git checkout HEAD -- package*.json next-env.d.ts && npm ci` → next@16/prisma@7/exceljs@4, **audit 8 moderate / 0 high / 0 critical**, build + tsc verts. Règle gravée `CLAUDE.md §Conventions` : jamais `--force` ; transitive précise = `overrides`. Les 8 moderates restantes = transitives dev/internes non-exploitables (postcss interne next, @hono via @prisma/dev, uuid via exceljs, dompurify via jspdf) → acceptées. **Docs-only** (1 ligne CLAUDE.md), 0 code, **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #364 | fix(cockpit) unifie campagne → actions → briefs → missions (BrandAction canonique) | NEFER (auto) — opérateur/fondateur | Correctif de mécanique produit hors phases 0–9. Bug : détail campagne canon affichait 0 action/0 mission alors que les cartes listaient des actions. Cause : **deux modèles d'action parallèles jamais reliés** — `BrandAction` (stratégique, ADR-0094/0119, rattaché par la génération canon) vs `CampaignAction` (média ATL/BTL/TTL, seul connu du détail). Fix unifié sur BrandAction : `getById` inclut `brandActions` ; service+procédure **gouvernée** `explodeActionToMission` (BrandAction → CampaignBrief PRODUCTION dérivé de l'action → Mission, idempotent) ; diagnostic `chainHealth` + compteurs campaign-scoped + empty-states honnêtes ; CampaignAction démoté en « Plan média ». **0 migration Prisma** (réutilise `Mission.briefData`/`CampaignBrief.content`/`BrandAction.status`), **0 nouveau Neter** (Cap APOGEE 7/7), **0 bypass** (IntentEmission hash-chaîné). Vérifié : tsc/eslint/2289 tests/build + test fonctionnel + **test UI live authentifié** (actions affichées, éclatement → mission créée). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #366 | fix(cockpit) « éclatée en mission » = mission réelle (briefData.brandActionId), pas statut calendaire | NEFER (auto) — opérateur | Suite directe de #364 (v6.27.56), correctif de production cockpit campagnes hors phases 0–9 (même nature que #357–#364). Bug : actions canon affichées « 3/3 éclatées » + « Mission creee » alors que `MISSIONS = 0` (bouton « Éclater » masqué). Cause : statut `SCHEDULED` surchargé — posé pour *dater au calendrier* (`setTiming`/`autoSchedule`, sans mission) ET lu comme « éclatée » par `getCampaignChainHealth` + le badge UI. Fix déterministe : helper pur `deriveExplodedActionIds` (vérité = `Mission.briefData.brandActionId`) ; `getCampaignChainHealth`/UI pilotés par les missions réelles ; `explodeBrandActionToMission` n'écrase plus le statut calendaire. **0 model Prisma**, 0 migration, 0 LLM, 0 bypass. Cap APOGEE 7/7. tsc 0 · eslint 0 · 2285 tests (5 cas régression). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #367 | feat(cockpit) pipeline staged campagne→brief→mission→pipes + fiche mission (activités/budget/KPI) | NEFER (auto) — opérateur | Suite directe de #364/#366. Refonte de mécanique produit cockpit campagnes/missions hors phases 0–9 (même nature que #357–#366). Câble le flux staged demandé bout-en-bout : « Éclater en mission » → « Générer le brief » (action→`CampaignBrief` DRAFT, éditable avant validation, budget définitif via `content.missionBudget`) ; validation → mission liée (`Mission.briefId`) ; fiche mission (Terminer/Annuler + Soumettre à La Guilde) ; **nouveau modèle `MissionActivity`** (couche d'exécution manquante : ASSET_CREATION/FIELD_ACTION + budget + KPI cible/réel + brief propre déterministe, complétion d'activité → progression/conclusion mission). `chainHealth` réaligné staged. **1 migration additive** (`Mission.briefId` + table `MissionActivity`, 0 destructif), déterministe, 0 LLM, 0 bypass (`governedProcedure`). Cap APOGEE 7/7 (Imhotep = pipe crew, La Guilde = portail ; aucun nouveau Neter). tsc 0 · eslint 0 erreur · 2290 tests (dont `deriveActionBriefs` + `computeMissionActivityHealth`). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #368 | fix(roadmap/forge) generateProjectsFromActions rerouté sur le flux staged (brief DRAFT, plus de mission auto) | NEFER (auto) — opérateur | Suite directe de #367. Réconcilie le dernier flux qui contournait la gate de validation : roadmap calendar + page Forge créaient via `generateProjectsFromActions` un brief **auto-VALIDATED** + une mission directe. Désormais : rattachement à la campagne (création PUNCTUAL pour orphelines, invariant ADR-0119) + génération du brief PRODUCTION **DRAFT** via le générateur staged `generateBriefFromBrandAction` (stampé `brandActionId`) ; plus de brief auto-VALIDATED ni de mission auto (la mission naît de la validation opérateur). Forme de retour préservée (`projects[].campaignId`) → callers intacts ; libellé Forge « Forger projets » → « Générer briefs » ; import `buildCampaignBrief` retiré ; réalignement allowlist KEYSTONE C5 (ligne `pillar.create` strategy.ts 83→82). Les 3 flux action→mission (détail campagne / roadmap / Forge) unifiés sur la gate staged. **0 migration**, déterministe, 0 LLM, 0 bypass. Cap APOGEE 7/7. tsc 0 · eslint 0 erreur · 2290 tests. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #370 | feat(campaigns) ADR-0120 — frames canon découplés des actions (gate = direction créative) | NEFER (auto) — opérateur | PR-1/4 du revamp de l'aspect opérationnel acté par l'opérateur (« Revamp direct », 2026-06-29). Hors phases 0–9 (même nature que #357–#368). Diagnostic du « 3 campagnes canon · 0 action rattachée » : l'amorçage canon ne **crée** jamais d'action — il rattachait les `BrandAction` déjà `selected:true` (promues seulement à la synthèse S). Enchaînement doctrinalement à l'envers. **[ADR-0120](adr/0120-production-pipeline-creative-proposal-gate.md)** (raffine ADR-0119) relocalise le trigger de création (Actions + briefs de prod) de l'Advertis vers la **validation de la direction créative** (Proposition Créative — Voie A La Fusée IA / Voie B La Guilde, même Data Contract). PR-1 livrée : `generateCanonicalCampaigns` n'attache plus d'action (frame = cadre stratégique ; le planner PUR garde `actionIds` pour le seul budget conseillé) ; UI honnête (« en attente de direction créative » au lieu de « 0 actions ») ; `routeKey` libellé niveau d'exécution (Conservateur/Cible/Ambitieux). Table anti-doublon dans l'ADR (quasi toutes les primitives existent ; seule l'entité Proposition Créative est neuve, PR-2). **0 migration**, 0 LLM, 0 bypass. Cap APOGEE 7/7. tsc 0 · eslint 0 · 2299 tests verts (canon planner 7/7 + ADR guards 4/4). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #371 | feat(campaigns) Proposition Créative — entité + Data Contract + gate (ADR-0120 PR-2) | NEFER (auto) — opérateur | PR-2/4 du revamp de l'aspect opérationnel (suite #370, hors phases 0–9). Implémente l'entité-gate du Nouveau Pipeline de Production (ADR-0120) : la **Proposition Créative** (seul élément vraiment neuf — tout le reste se rebranche). Nouveau modèle `CreativeProposal` (migration **additive** `20260629140000`, 0 modèle existant touché) + **Data Contract** Zod (`creative-proposal.ts`, forme identique Voie A IA ≡ Voie B Guilde, seul `source` diffère) + gate `validateCreativeProposal` qui **génère actions + briefs de production dans les frames canon** au niveau d'exécution choisi (réutilise `routeInitiativeSet` ADR-0089 + `generateBriefFromBrandAction` — relocalisation du trigger Advertis→proposition). 4 Intent kinds gouvernés + SLOs + manifest + router tRPC + UI panel cockpit (Voie B manuelle baseline). Déterministe, **0 LLM**, 0 bypass (`governedProcedure` + `mestor.emitIntent`). Cap APOGEE 7/7 (aucun nouveau Neter). tsc 0 · eslint 0 · madge 0 cycle · 2304 tests verts (5 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #373 | feat(campaigns) Voie A déterministe — preview des 3 niveaux d'exécution (ADR-0120 PR-3) | NEFER (auto) — opérateur | PR-3/4 du revamp opérationnel (suite #370/#371, hors phases 0–9). Surface le cœur **déterministe** de la Voie A : les 3 niveaux d'exécution (Conservateur/Cible/Ambitieux, dérivés de l'Advertis via `computeRoadmapRoutes` ADR-0089) dans le formulaire de Proposition Créative. `getExecutionLevels` + query tRPC `executionLevels` : pour chaque niveau, compte + budget des actions que sa validation rattacherait **réellement** (`routeInitiativeSet` sur les `BrandAction` matérialisés — même calcul que `validateCreativeProposal`) + growth Pilier S. Helper pur `summarizeExecutionLevels` (testé) + UI 3 cartes informatives. **Choix mandat-aligné « minimiser le LLM »** : décision-support 100 % déterministe ; génération IA direction + image + portail La Guilde = suivi explicite. **0 migration**, **0 nouvel Intent**, 0 LLM, 0 bypass. Cap APOGEE 7/7. tsc 0 · eslint 0 · 2305 tests verts (1 nouveau). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #374 | feat(cockpit) fiche mission — auto-seed activités + assignation prestataire + brief éditable (ADR-0120 PR-4a) | NEFER (auto) — opérateur | PR-4a/4 du revamp opérationnel (suite #370/#371/#373, hors phases 0–9). Première tranche de la cascade aval — replie la WIP `mission-fiche-revamp` (backend déjà construit) + complète l'UI. `createMissionFromValidatedBrief` auto-seed les activités (`seedDefaultMissionActivities` : 1 asset + 1 terrain si touchpoint offline, budget réparti, cible KPI extraite via `extractLeadingQuantity`, label KPI depuis l'AARRR — déterministe, idempotent). Bouton **Régénérer** (`regenerateMissionActivities`). **Assignation prestataire par activité** (`assignMissionActivity`, miroir `mission.assign`, réutilise `suggestTalent`). **Brief d'activité visible + éditable** (cliquable → textarea → `updateActivityBrief`). Schéma : `MissionActivity.assigneeId` + index (migration **additive** `20260629150000`, renumérotée après `140000`). 2 Intent kinds `LEGACY_*` (regenerate/assign, exempts de registration comme les `LEGACY_MISSION_*_ACTIVITY` de #367). **0 modèle nouveau**, déterministe, 0 LLM, 0 bypass (`governedProcedure`). Cap APOGEE 7/7. Rétroplanning T0 + dashboards = PR-4b. tsc 0 · eslint 0 · 2307 tests verts (3 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #376 | feat(cockpit) rétroplanning mission T0 + durée activité fixée/dérivée (ADR-0120 PR-4b) | NEFER (auto) — opérateur | PR-4b/4 du revamp opérationnel (suite #370/#371/#373/#374, hors phases 0–9). Moteur de rétroplanning ancré sur T0. **Durée d'activité à 2 options** (décision opérateur 2026-06-29) : FIXÉE (`MissionActivity.durationDays`) sinon **DÉRIVÉE** déterministe par type (`resolveActivityDurationDays` ; défauts ASSET_CREATION 5 j / FIELD_ACTION 3 j). Moteur PUR `computeRetroplan` (`retroplan.ts`, zéro LLM) : enchaîne les activités (ordre) dans une fenêtre **qui se termine à T0** (dernière finit à T0, 1ère démarre à T0 − total) ; offsets J-N + dates ISO + flag dérivé ; annulées ignorées. Service `getMissionRetroplan` (T0 = lancement campagne `startDate` → SLA → today → override) + `setMissionActivityDuration`. tRPC `mission.retroplan` (query) + `mission.setActivityDuration` (mutation, 1 `LEGACY_*` kind exempt). UI : section Rétroplanning (timeline J-N + dates + durée auto/fixée) + input durée par activité. Migration **additive** `20260629160000` (champ nullable). **0 modèle nouveau**, déterministe, 0 LLM, 0 bypass. Cap APOGEE 7/7. Macro Roadmap = suivi PR-4c. tsc 0 · eslint 0 · 2312 tests verts (5 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #372 | fix(intake) diagnostic ADVE « Load failed » réparé (route maxDuration + owl-alpha défaut) + feat(llm-gateway) owl par défaut / toggle premium / embeddings OpenRouter dégradables | NEFER (auto) — opérateur | Correctif de production du funnel intake + feature résilience LLM hors phases 0–9 (même nature que #299/#300 hotfixes). Cause « Load failed » : route tRPC `/api/trpc/[trpc]` **sans `maxDuration`** → `processIngest` + `complete` (~100-130 s) tués au plafond serverless (étalon Xtincell figé IN_PROGRESS, `updatedAt==createdAt`, réponses vides) ; aggravé par le modèle fictif `gpt-5.5` codé en dur (détour Anthropic 404 + circuit-breaker avant owl) + `AbortController` mort + scrape/Brave/LLM séquentiels. Fixes : `maxDuration=300`+`runtime=nodejs` sur la route ; `extractFromText` → owl-alpha (`purpose:"extraction"`, `responseFormat:"json_object"`, signal réel câblé, parse `extractJSON`) ; scrape site + Brave **concurrents et bornés** (helpers `fetchUrlAsText`/`fetchDigitalPresenceBlock`, de-dup). feat gateway : **owl-alpha = modèle texte par défaut** (`resolveTextProviderOrder` pur+testé) + toggle `LLM_PREMIUM_MODE` (Anthropic une fois crédits chargés) + **embeddings OpenRouter dégradables** (no-op crédit épuisé) + `abortSignal` propagé. **0 migration, 0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass governance**. tsc 0 · eslint 0 · gateway+intake+rate-policy tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #377 | feat(cockpit) Macro Roadmap — timeline des campagnes (ADR-0120 PR-4c) | NEFER (auto) — opérateur | PR-4c/4 — **clôt la cascade aval** du revamp opérationnel (suite #370/#371/#373/#374/#376, hors phases 0–9). Vue Macro Roadmap (page Campagnes) : timeline horizontale des campagnes (canon + ponctuelles) positionnées proportionnellement à leurs dates, ligne « aujourd'hui », always-on en bande jusqu'au bout de fenêtre, sans-date listées à part. Helper **PUR** `computeRoadmapLayout` (`lib/strategy/roadmap-layout.ts`, client-safe, zéro dép : fenêtre min→max, leftPct/widthPct, nowPct ; testé 4 cas). `MacroRoadmapPanel` read-only consommant `campaign.list` **existant** (aucune nouvelle requête, aucun modèle). Micro-dashboard (santé activité) déjà en place (PR-4a). **0 migration · 0 nouveau modèle · 0 nouvel Intent · 0 LLM · 0 bypass** (lecture seule). Cap APOGEE 7/7. PR-4 bouclé ; suivi déféré : Voie A IA (Glory + KV image) + portail La Guilde. tsc 0 · eslint 0 · 2316 tests verts (4 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #378 | feat(campaigns) Voie A IA — brouillon de direction depuis l'ADVE (ADR-0120 PR-5) | NEFER (auto) — opérateur | Sort la **Voie A IA** du suivi déféré (suite #370–#377, hors phases 0–9). Bouton « Pré-remplir avec l'IA » dans la Proposition Créative : `draftCreativeDirectionFromStrategy` charge A/D/V + `executeStructuredLLMCall` (ADR-0067) → brouillon direction (Big Idea/insight/axe/pistes). **Ne persiste rien** : pré-remplit le formulaire, l'opérateur corrige avant le `create` déterministe (**manual-first parity ADR-0060**, Voie B utilisable sans IA, Gateway-down → dégradation propre). Mirroir du précédent béni `GUILD_DRAFT_MISSION_FROM_TEXT` (seule entrée LLM). `creativeDirectionDraftSchema` (Zod lenient) + Intent gouverné `DRAFT_CREATIVE_PROPOSAL_FROM_STRATEGY` + SLO + manifest (`LLM_CALL`) + tRPC `creativeProposal.draftDirection`. **Seule entrée LLM** du pipeline (cœur déterministe). **0 migration · 0 nouveau modèle · 0 bypass**. Cap APOGEE 7/7. Reste credential-gated : visuels image API (Ptah/Magnific) + portail public La Guilde. tsc 0 · eslint 0 · 2324 tests verts (2 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #379 | feat(laguilde) Voie B — soumission de direction créative par un membre guilde (ADR-0120 PR-6) | NEFER (auto) — opérateur | Branche **La Guilde comme Voie B** du pipeline de Proposition Créative (suite #370–#378, dernier item déféré non credential-gated). Un membre guilde **assigné à ≥1 mission** d'une stratégie soumet une direction créative (Big Idea/insight/axe/pistes) → proposition **SUBMITTED** dans la file opérateur, traitée par la gate `validateCreativeProposal` existante (même Data Contract que la Voie A IA, seul `source=LAGUILDE_HUMAN` diffère). Accès gardé par **lien mission** (`Mission.assigneeId`) — pas de chicken-and-egg, la direction initiale reste opérateur/Voie A ; La Guilde propose pour les marques qu'elle sert déjà. `listGuildProposableStrategies` + `submitGuildCreativeProposal` (helper pur testé `dedupeProposableStrategies`) + Intent gouverné `GUILD_SUBMIT_CREATIVE_PROPOSAL` + SLO + manifest + tRPC (`guildProposableStrategies`/`submitGuildProposal`) + UI creator `/creator/proposals` (+ nav « Propositions »). Les deux voies (A IA #378 · B Guilde) existent désormais bout-en-bout, même gate. **0 migration · 0 nouveau modèle** (réutilise `CreativeProposal` `source=LAGUILDE_HUMAN` + `Mission`) · **0 bypass** (`governedProcedure` + `mestor.emitIntent`). Cap APOGEE 7/7 (Imhotep = pipe crew, La Guilde = portail ; aucun nouveau Neter). Reste credential-gated : génération de visuels (image API). tsc 0 · eslint 0 · 2326 tests verts (2 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-29 | #380 | docs(cockpit) audit UX end-to-end + remédiation Lot 1 (anti-fuite de vocabulaire interne, libellés client) | NEFER (auto) — opérateur/fondateur | Audit hors phases 0–9 (même nature que #363) **+ remédiation Lot 1**. Audit : `docs/audits/COCKPIT-UX-AUDIT-2026-06-29.md` — revue 50 pages `(cockpit)` en 8 clusters + audit de flow bout-en-bout (onboarding · cascade ADVE→RTIS→Oracle · S→production · boucle de mesure · valeur/tier/paiement). Remédiation Lot 1 (Décision 1 vocabulaire) : **libellés client uniquement** sur 16 fichiers (nav i18n fr/en/zh, sidebar, breadcrumb, command-palette, topbar, dock Recommandations, dashboard, page Assistant, pages Recommandations/Gazette, libellés de champs piliers) — purge des fuites Mestor→Assistant, Notoria→Recommandations, Jehuty→La Gazette, « Séquences Glory »→« Séquences », « NETERU »/« Le Messie »/« Compétences Divines »/« (GLORY) »/« Cult Dashboard/Index ». **0 changement de logique métier, 0 model Prisma, 0 nouveau Neter** (Cap APOGEE 7/7), **0 bypass governance** (aucune mutation/service/Intent touché — chaînes d'affichage seulement). **Lot 2 (Décision 2, UI honnête par rôle)** : `auth.me` expose un champ additif `canOperate` (miroir exact de `operatorProcedure` init.ts — ADMIN ou `operatorId`) ; la page Recommandations désactive les contrôles opérateur + affiche une bannière lecture-seule pour les fondateurs, au lieu de l'erreur `FORBIDDEN` au clic. **Lot 3** étend ce gating honnête (hook réutilisable `useCanOperate`) au **panneau Oracle progressif** (boutons assemblage/section désactivés + bannière ; le `enrichOracle` legacy gouverné reste utilisable par le fondateur) et à l'**amendement de pilier** (le fondateur est routé vers l'éditeur `/cockpit/brand/edit` au lieu de la modale operator-only). **Lot 4 (actions honnêtes, affichage)** : fin des actions no-op/mensongères — composeur Forge présenté comme un *aperçu de cascade* (plus de faux « Composition complétée » ni « 100% de confiance », plus de jargon « Phase 17/IntentEmission/NSP »), bouton « PDF » des Rapports relibellé « Texte » (génère un `.txt`), export liste des livrables relibellé « Exporter (JSON) ». **Lot 5 (honnêteté des données)** : le *Devotion Ladder* du dashboard (surface d'accueil) n'affiche plus une distribution codée en dur (35/25/20/12/5/3 %) en l'absence de donnée — EmptyState honnête à la place (canon « ne jamais inventer de données ») ; titre relibellé « Échelle d'engagement ». **Lot 6 (sécurité — IDOR)** : les 2 routes d'export REST (`/api/export/oracle/[id]/pdf` + `/api/export/[id]`) ne vérifiaient que l'authentification, pas la propriété → tout utilisateur connecté pouvait télécharger l'Oracle / les données complètes de n'importe quelle marque par ID. Ajout du garde `canAccessStrategy` (helper canonique `operator-isolation`, miroir exact du strategy router : ADMIN, propriétaire `userId`, ou même opérateur ; sinon 403). **Durcissement** (ajoute une garde, ne touche ni service métier ni Intent ni schéma). **Lot 7 (honnêteté des données, résidu)** : Newsletter « délivrabilité 100% » codée en dur → « — / bientôt disponible » (+ retrait « Anubis ») ; Operations Center KPI « Campagnes Actives » trend toujours « up » → « flat » ; checklist Forge tout-en-vert → critères neutres (plus de fausses coches vertes) ; Benchmarks — suppression de la différenciation par pilier inventée (formule char-code) au profit de l'agrégat secteur réel (notice « simulé » conservée). **Lot 8 (onboarding + valeur)** : dashboard zéro-marque → EmptyState « Créez votre première marque » (CTA `/cockpit/new`) au lieu d'un squelette infini (fin du cul-de-sac premier-run) ; `/cockpit/new` atterrit sur la fiche de marque réelle (plus de redirection `?boot=` morte, `sessionId` toujours « started ») + fausse promesse « Boot Sequence » remplacée par une copie honnête (onboarding profond opéré par UPgraders, cohérent Décision 2) ; nouvelle section **Abonnement** dans `/cockpit/settings` (consomme la procédure existante `payment.mySubscriptions` — offre/statut/montant/échéance, bannière honnête `pending_manual`, lien `/pricing` si aucun). **Lot 9 (sweep vocabulaire — fuites P0 résiduelles)** : réécriture client de la pire surface de l'audit `campaigns/[id]/tracker` (suppression APOGEE/Thot/Seshat/Loi 1·3/Cluster A·B/L2 Instrumental/Jaccard/refs ADR/Intent kinds/enums bruts → « Trajectoire », « Évolution du palier », « Consommation du budget », « Fenêtre de vigilance », « Écart à la promesse », « Continuité du récit » ; + fil d'Ariane retour ; + map FR de l'état budget) ; forge (« sous ADVERTIS »/« forge Ptah final »/« LLM tools » → business) ; newsletter (« via Anubis » retiré) ; market-studies (« moteur Tarsis »/« KnowledgeEntry »/« pilier T » → « moteur d'analyse »/« veille marché »). **Aucun desserrement de gouvernance** (la gate serveur operator-only reste inchangée ; on rend juste l'UI honnête), 0 model Prisma, 0 nouveau Neter (Cap APOGEE 7/7), 0 décision structurelle (pas d'ADR). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-30 | #384 | fix(os) healer pass — purge erreurs UI/fonctionnelles + résilience LLM + SEO | NEFER (auto) — opérateur | Passe de maintenance transverse mandatée par l'opérateur (« ne laisser aucune erreur avant le passage public » + « ne rien laisser en planifié »), hors phases 0–9 (même nature que #372). Robot `scripts/heal/` (orchestrateur self-heal + **scan fonctionnel** déclenchant tous les noeuds Glory/sequences/frameworks). Correctifs : **résilience LLM** (OpenRouter en fallback + Ollama Cloud `deepseek-v4-flash` primaire local + **sanitisation null-bytes** qui crashait le persist jsonb + fix `OPENAI_MODEL_MAP` `gpt-5.5`→`gpt-4o`) ; **sequences** — 9 Glory tools `COMPOSE` (`sequence-gap-tools.ts`) comblent les refs fantômes (0 step « GLORY inconnu ») + 3 `planned()` clôturés ACTIVE (BAIN-NPS/IMHOTEP-CREW/ANUBIS-COMMS), **doctrine Artemis→Ptah respectée** (générateurs de brief, aucun `forgeOutput`), bindings recadrés `d.personas`/`e.aarrr` (évite des requirements COMPLETE fantômes `v.cibles`/`s.metriquesCles` qui auraient plafonné le score V/S de **toutes** les marques) ; **Oracle** (cascade staleness `markAllSectionsStale` + writeback déterministe `composeSectionDeterministic` + §32 lit `evaluations`) ; **SEO socle** (`sitemap.ts` dynamique + `opengraph-image` + metadata/JSON-LD) ; **outillage** (fix `eslint-plugin-lafusee` backslashes Windows, command-palette routes, breadcrumb guard, fixes hydration). **0 migration · 0 nouveau Neter · 0 nouveau modèle Prisma · 0 bypass governance**. Cap APOGEE 7/7. tsc 0 · eslint 0 · 2326 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-30 | #385 | fix(oracle) ORACLE_DERIVED calc recharge via PRESENTATION_INCLUDE | NEFER (auto) — opérateur | Suite directe du healer pass #384 (purge d'erreurs Oracle, hors phases 0–9). Les 7 sequences `ORACLE_DERIVED` rechargeaient la strategy (`executeSectionDraftCalc`) avec un include minimal hand-rollé alors que les `section-mappers` sont co-conçus avec `PRESENTATION_INCLUDE` et accèdent SANS garde à `m.deliverables.map`/`strategy.gloryOutputs` (mapProductionLivrables) + `strategy.user.name` (mapConditionsEtapes) → throws → DERIVED-PROD-LIV/DERIVED-CONDITIONS PARTIAL au scan. Fix : `PRESENTATION_INCLUDE` exporté + réutilisé (superset prouvé par la voie enrich prod) ; ferme la classe « mapper accède à une relation non incluse ». Test HARD `oracle-derived-include.test.ts` (relations critiques + réutilisation). **0 migration · 0 nouveau modèle · 0 nouveau Neter · 0 bypass**. Cap APOGEE 7/7. tsc 0 · eslint 0 · oracle/sequence tests verts (52 + 5 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-30 | #386 | fix(ptah) forge différée (DEFERRED) si provider non configuré — ship-able sans clés | NEFER (auto) — opérateur | Suite du healer pass #384/#385. Audit de la décorrélation Artemis(brief)→Ptah(forge) demandé par l'opérateur : architecture SAINE (Glory tools = briefs, `chainGloryToPtah` émet `PTAH_MATERIALIZE_BRIEF`, aucun tool ne forge directement). Vrai défaut : `materializeBrief` appelait `provider.forge()` sans credentials → adobe/canva/figma throwaient → task FAILED + erreur propagée. Ptah était le SEUL connecteur à ne pas honorer le pattern `DEFERRED_AWAITING_CREDENTIALS` (ADR-0021, ship-able sans clés). Fix : pré-flight `provider.isAvailable()` → `markDeferred` (status DEFERRED, retriable) sans throw ; Magnific reste dispo (mock fallback). 5 surfaces (type/service/store/tRPC/UI StatusPill tokens DS). Test `ptah-provider-availability.test.ts` (déclencheur deferral). **0 migration** (status String). Cap APOGEE 7/7. tsc 0 · eslint 0 · ptah tests verts (17 + 3 nouveaux). Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-30 | #387 | fix(ui) ptah-forge-runner StatusPill → tokens DS (interdit #2) | NEFER (auto) — opérateur | Nettoyage opportuniste du fichier touché en #386 : couleurs Tailwind brutes (amber/emerald/zinc-500) du StatusPill → tokens sémantiques DS (warning/success/foreground-secondary). Aligne sur l'interdit #2 (aucune classe couleur brute hors primitives/styles). **Note honnête** : la migration DS complète est *codemod-scale* (≈53 fichiers / 382 occurrences, mapping sémantique contextuel) — ce PR ne couvre que ce fichier ; le reste reste dette baselinée (lint-clean), à traiter par codemod dédié. **0 migration · 0 nouveau modèle · 0 bypass**. Cap APOGEE 7/7. eslint 0 · DS governance tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-30 | #389 | feat(artemis) Bible de Marque — deck 16:9 téléchargeable (2ᵉ livrable) | NEFER (auto) — opérateur | Feature net-new mandatée par l'opérateur (2ᵉ livrable prioritaire après l'Oracle), hors phases 0–9. Compile les Glory tools BRAND-layer (séquence BRANDBOOK-D) en deck 16:9 téléchargeable. **Réutilisation maximale** : données via `compileDeliverable(strategyId,"BRANDBOOK-D")` existant + rendu jsPDF (serverless-safe comme `export-oracle.ts`, route auth-gardée miroir Oracle — pas de Chromium/page publique). Deck = couverture + 1 slide/étape BRANDBOOK-D (séquence = colonne vertébrale) ; section présente → contenu (walk JSON), absente → **empty-state honnête** (jamais de données inventées, PROPAGATION-MAP). **Doctrine Artemis→Ptah respectée** (compile des briefs, ne forge pas d'image). Surfaces : kind `BRAND_BIBLE` (+ KIND_TO_PILLAR→D) · exporter · route `/api/export/brand-bible/[strategyId]/pdf` · bouton Cockpit. **0 migration** (kind = String) · 0 nouveau Neter · 0 nouveau modèle · 0 bypass. Cap APOGEE 7/7. Test `brand-bible-deck.test.ts` (spine + rendu %PDF-). tsc 0 · eslint 0 · 3 nouveaux + 34 kind-tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-30 | #390 | feat(ptah) OpenAI = générateur d'image exclusif (gpt-image-1, synchrone) | NEFER (auto) — opérateur | Directive opérateur (« l'API de génération d'image, c'est OpenAI exclusivement », clé fournie), hors phases 0–9. Câble OpenAI Images comme provider Ptah canonique pour forgeKind image/icon (édition/vidéo/audio = Magnific). Provider `gpt-image-1` (vérifié EN LIVE : dall-e-2/3 absents, response_format rejeté → b64), SYNCHRONE (flag `ForgeProvider.sync` → `materializeBrief` réconcilie inline, pas de webhook), persistance b64 dual-mode (blob storage si `BLOB_STORAGE_PUT_URL_TEMPLATE`, sinon data URL out-of-box). Routing `KIND_TO_PROVIDER` image/icon → `["openai"]` exclusif. **Corrige un angle mort de #386** : `selectProvider` lève `NoAvailableProviderError` (garde post-sélection de #386 inatteignable) → `materializeBrief` catch → forge différée (ADR-0021) au lieu de crasher ; garde morte retirée. **0 migration** (provider/status = String) · 0 nouveau Neter · 0 nouveau modèle · 0 bypass. Cap APOGEE 7/7. Test `ptah-openai-image-provider.test.ts`. tsc 0 · eslint 0 · 2342 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-06-30 | #391 | feat(ptah) gpt-image-1/2 + fix(llm-gateway) embeddings Ollama-aware | NEFER (auto) — opérateur | Réglages modèles externes (directive opérateur, clés fournies), hors phases 0–9. (1) **gpt-image-1 1K défaut + gpt-image-2 2K opt-in** (workflow opérateur, même prompt ; pilotable par brief `parameters.model`/`resolution="2K"`). (2) **Embeddings Ollama-aware** : constat vérifié EN LIVE — ollama.com n'héberge que des modèles chat/code (35, AUCUN embedding ; /v1/embeddings → "path not found") → embeddings basculent sur OpenAI (text-embedding-3-small, dim 1536 vérifié). `embedViaOllama` gère désormais OpenAI-compat (/v1/embeddings+Bearer) ET natif (/api/embeddings) ; `selectEmbedProvider` lit `EMBED_PROVIDER` (pin `openai` pour éviter une tentative Ollama vouée à l'échec). **0 migration · 0 nouveau modèle Prisma · 0 nouveau Neter · 0 bypass.** Cap APOGEE 7/7. tsc 0 · eslint 0 · provider tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-07-01 | #392 | feat(artemis) bible de marque — planches visuelles (placeholder + prompt dans le cadre) | NEFER (auto) — opérateur | Itération sur le 2ᵉ livrable (#389) demandée par l'opérateur (« c'est la mise en forme qui m'intéresse, mets des placeholders avec le prompt dans le cadre »), hors phases 0–9. Le deck 16:9 passe de slides texte à PLANCHES VISUELLES : cadre placeholder « VISUEL À FORGER · gpt-image-1 (1K) » avec le PROMPT laser dans le cadre + colonne DIRECTIONS DU BRIEF ; image forgée embarquée (jsPDF addImage) si attachée au brief. `buildPlatePrompt`/`extractImageDataUrl`/`renderVisualFrame`/`renderCaption` + `exportBrandBibleAsPdf(strategyId,{manifestOverride})` (rendu depuis manifest fourni, démos sans DB). Échantillon Mammy Watta (13 planches) généré + livré à l'opérateur pour validation mise en forme avant case study. **0 migration · 0 nouveau modèle · 0 bypass.** Cap APOGEE 7/7. tsc 0 · eslint 0 · brand-bible tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-07-01 | #393 | feat(llm-gateway) embeddings Ollama local + fix(campaigns) amorçage multi-axes | NEFER (auto) — opérateur | Deux branchements mandatés par l'opérateur, hors phases 0–9. (1) **Embeddings via Ollama LOCAL** (`nomic-embed-text-v2-moe`, dim 768) le temps du VPS : découplage réseau `EMBED_SERVICE_URL` (chat cloud deepseek ↔ embeddings `localhost:11434` sans auth) — supersede le pin OpenAI de v6.27.72 (Ollama Cloud n'a AUCUN endpoint d'embedding, re-vérifié 3 façons ; le modèle est library-only). Vérifié end-to-end : `provider=ollama dim=768`. (2) **Fix gate direction créative** : les frames canon ne seedaient aucune `CreativeProposal` → gate vide. `seedCreativeAxesIfEmpty` seed 2 axes distincts (Voie A IA `angleHint` + fallback déterministe), panel auto-amorce. Nouveau kind gouverné `SEED_CREATIVE_AXES` (intent-kinds + manifest + SLO). **0 migration** (env + additif) · 0 nouveau Neter · 0 bypass. Cap APOGEE 7/7. tsc 0 · eslint 0 · 2342 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-07-01 | #395 | feat(ptah) forge conditionnée par image de référence (gpt-image edits) + routing image exclusif | NEFER (auto) — opérateur | Fondation du système de dépendance du case study (audit opérateur « tools gpt-image + gestion assets + banques de brief + découplage »), hors phases 0–9. Audit : pipeline sain SAUF 2 trous. (1) **Reference-image conditioning** : `openai.ts` route vers `/v1/images/edits` (multipart `image[]`, ≤16 refs) si `parameters.referenceImageUrls` présent (URLs/data-URLs), sinon `/v1/images/generations` ; vérifié EN LIVE bout-en-bout. (2) **Routing image exclusif** : `provider-selector` n'honore un `providerHint` que s'il est autorisé pour le kind → un `providerHint:"magnific"` périmé sur les tools image/icon ne court-circuite plus le routage image→openai (magnific mock isAvailable=true). **0 migration** (param additif) · 0 nouveau Neter · 0 bypass. Cap APOGEE 7/7. tsc 0 · eslint 0 · 2343 tests verts. Cf. Justification — out-of-scope dans le body PR. |
| 2026-07-05 | #419 | docs(revamp) cahier des charges v2 + prompt de rebuild ground-level | NEFER (auto) — opérateur | Artefacts opérateur pour le revamp ground-level de La Fusée dans un **repo GitHub neuf** (décision opérateur 2026-07-05) : `revamp/CAHIER-DES-CHARGES.md` (spec auto-suffisante distillée de l'audit code v6.27.43 — intention + fonctionnalités réellement développées + contrat de portabilité + anti-patterns v1) + `revamp/REVAMP-PROMPT.md` (prompt d'exécution Claude Fable 5). Docs uniquement, hors phases refonte 0-9. **0 nouveau Neter** (Cap APOGEE 7/7), **0 model Prisma**, **0 bypass**, 0 code applicatif touché. Cf. Justification — out-of-scope dans le body PR. |
| 2026-07-07 | #428 | fix(auth) reconduction post-login cassée + redirection canonique de domaine | NEFER (auto) — opérateur | Deux bugfixes isolés sur `src/proxy.ts` (garde d'authentification + routing), hors phases 0–9. (1) **Reconduction post-login** : login réussi sur `/login?callbackUrl=/console` mais pas de reconduction, boucle retour `/login`. Root cause : `getToken()` d'Auth.js dérive `cookieName` ET `salt` depuis `secureCookie` par défaut (même valeur — le nom du cookie sert de salt de dérivation) ; le code passait un `salt` explicite sans pinner `cookieName`, désynchronisant les deux — le fallback legacy (`__Secure-next-auth.session-token`) déchiffrait donc toujours avec le mauvais salt (échec silencieux, `catch` avalé). Simplifié en un seul appel `getToken({ req, secret, secureCookie })` (dérivation cohérente, usage documenté `@auth/core/jwt`) + détection HTTPS robustifiée via `x-forwarded-proto` (backend HTTP derrière la terminaison TLS Coolify/Traefik). (2) **Redirection canonique de domaine** : l'app Coolify répond directement (200, pas de redirect) sur 3 domaines (`powerupgraders.com` / `www.` / `lafuseev6.`) — vérifié EN LIVE. Conséquence concrète repérée par l'opérateur : la bannière cookie-consent (`localStorage`, scopé par origine) réapparaît à chaque atterrissage sur une variante différente. Ajout d'un 308 vers `powerupgraders.com` (env `CANONICAL_HOST` overridable) pour tout host ≠ canonique, prod-only (`NODE_ENV=production`), matcher élargi à toutes les routes navigables (était limité aux routes protégées/legacy — `/` et `/intake` n'étaient jamais passées au middleware). **0 migration · 0 nouveau Neter · 0 nouveau modèle · 0 bypass.** Cap APOGEE 7/7. tsc 0 · eslint 0. Cf. Justification — out-of-scope dans le body PR. |
| 2026-07-08 | #NEXT | fix(seshat) ranker/oracle-augment filtrent aussi par embeddingModel (pas dim seul) | Claude (opérateur Alexandre) | Durcissement post-migration embedding : un store homogène nomic-embed-text:v1.5/768 étant rétabli, on empêche tout futur mélange de géométries de même dimension dans une recherche cosinus. 0 migration, additif. |
| 2026-07-10 | #398 | chore(actions) bump actions/checkout 4→7 (dependabot) | Claude (opérateur Alexandre) | Maintenance CI hors phases 0–9 : bump du runner checkout sur les 13 fichiers de workflows (v6→v7 après auto-rebase dependabot). 0 code applicatif · 0 migration · 0 modèle. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-10 | #399 | chore(actions) bump chromaui/action 17→18 (dependabot) | Claude (opérateur Alexandre) | Maintenance CI hors phases 0–9 : bump de l'action Chromatic (workflow visual review uniquement). 0 code applicatif · 0 migration · 0 modèle. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-10 | #361 | chore(deps) bump zod 4.4.2→4.4.3 (dependabot) | Claude (opérateur Alexandre) | Maintenance deps hors phases 0–9 : patch de la lib de validation (aucun changement d'API). 0 code applicatif · 0 migration · 0 modèle. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-10 | #421 | chore(deps) bump groupe eslint — eslint 10.6, config-next 16.2.10, boundaries 7 (dependabot) | Claude (opérateur Alexandre) | Maintenance deps hors phases 0–9 : outillage lint (devDependencies), aucun impact runtime. 0 code applicatif · 0 migration · 0 modèle. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-10 | #283 | chore(deps) bump groupe trpc 11.17→11.18 — client/next/react-query/server (dependabot) | Claude (opérateur Alexandre) | Maintenance deps hors phases 0–9 : 4 minors alignés du framework RPC. 0 code applicatif · 0 migration · 0 modèle. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-10 | #285 | chore(deps) bump groupe tailwind — @tailwindcss/postcss 4.3.2 (dependabot) | Claude (opérateur Alexandre) | Maintenance deps hors phases 0–9 : patchs chaîne de build CSS ; Chromatic + build en filet. 0 code applicatif · 0 migration · 0 modèle. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-10 | #362 | chore(deps) bump puppeteer 24.42→25.2.1 (dependabot) | Claude (opérateur Alexandre) | Maintenance deps hors phases 0–9 : majeur du moteur PDF (money-path) — risque analysé (Node 22 OK, ESM-only compatible avec l'import dynamique, PUPPETEER_EXECUTABLE_PATH honoré) et vérifié empiriquement : launch+page.pdf (forme d'appel exacte du value-report-generator, chromium système + env comme en prod) → %PDF- valide sous 25.2.1. Vérif post-deploy sur /api/intake/<token>/pdf à faire par l'opérateur. 0 migration · 0 modèle. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-11 | #447 | fix(intake) chemin court honnête + site déclaré une seule fois | Claude (opérateur Alexandre) | Bugfix UX funnel public hors phases 0–9 : bascule import→questionnaire muette éliminée (pré-flight isTextLLMAvailable, retry, 8192 tokens, contrat null — jamais de squelette {biz:{},…} persisté, raison explicite + bandeau), site web pré-rempli + coalesce input ?? intake.websiteUrl (plus de null-erase de la déclaration), getQuestions aligné hasSubstantiveAnswer. Test anti-drift HARD intake-no-empty-skeleton (8 tests). 0 migration · 0 modèle · 0 bypass. Cap APOGEE 7/7. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-11 | #447 | fix(governance) ADR-0124 spine d'émission unifié (2ᵉ vague même PR) | Claude (opérateur Alexandre) | Mandat opérateur « répare le problème de fond » suite à l'inspection du noyau : emitIntent (chemin bus) n'avait ni hash-chain ni cost-gate ni événements ni persistance bloquante — les invariants Q1/Q2 et Lois 1/3 ne valaient que sur le chemin tRPC. Spine canonique governance/emission-spine.ts consommé par les deux chemins, fail-closed, vérificateur de chaîne réparé (recompute result:null), observation 4 états terminaux, recordCost vivant, writers directs reroutés ou allowlistés (pattern C5). Hors phases 0-9 stricto sensu mais nature phase/3 (Mestor/governed procedures) ; gate out-of-scope conservé : label + justification body + trace ici. 0 migration · 0 modèle · test HARD 11 assertions. Cap APOGEE 7/7. |
| 2026-07-11 | #447 | merge #448 + résiduels (a)-(f) audit UX Cockpit v2 (3ᵉ vague même PR) | Claude (opérateur Alexandre) | Mandat opérateur « intègre les efforts de l'autre dev et termine le restant » : merge de claude/brand-cockpit-ux-review-stw2ze (lots 10-15, nav ADR-0122, vocab ADR-0123), collisions résolues first-come (ADR spine → 0124, CHANGELOG 84-88 → 86-90). Résiduels clos : requireOperator durci 51 sites + 2 gardes ownership · allowlist vocab vidée (rtis→redirects, apogée purgée) · funnel public aligné ADR-0123 + verrou CI étendu · 0 prompt() natif · legacy enrichOracle déposé (ADR-0125) · campagne [id] découpée en campaign-tabs/ (iso-comportement, tab bar 5 jobs). Hors phases 0-9 ; gate out-of-scope : label + justification body + trace ici. Cap APOGEE 7/7. |
| 2026-07-11 | #450 | docs(governance) vérité unique + skills NEFER agent autonome | Claude (opérateur Alexandre) | Mandat opérateur post-merge #447 : « corrige la documentation (commentaire de code inclus) pour unifier le savoir, purge tout pour ramener à une vérité unique, conçois des skills rigides alignés à la configuration finale de NEFER ». Purge des 15 tensions inventoriées à l'inspection noyau (bible 11 anchors, contextes, maps, commentaires mensongers, i18n client, comptes recalculés sur registres) + alignement Devotion Ladder canon 6 rungs jusque dans le Glory tool §33 + suite .claude/skills/ (7 skills protocole 8+1 phases) + NEFER.md configuration finale (deux lanes, §11 skills). Hors phases 0-9 (docs + hygiène) ; gate out-of-scope : label + justification body + trace ici. 0 migration · 0 modèle · 0 bypass. Cap APOGEE 7/7. |
| 2026-07-11 | #451 | chore/docs — drift post-merge PR #450 (Phase 9 nefer-postmerge) | Claude (opérateur Alexandre) | Première exécution réelle du skill nefer-postmerge après merge #450 : rescan 9.2-9.5 → 3 drifts fixés — version unique (package.json/lock 6.27.43 → 6.27.93 + footer landing v6.27.5·2026-06-16 → v6.27.93·2026-07-11), compteurs NEFER.md §4 (350+ Intent kinds → 546, 85+/75+ services/routers → 115/112, Glory 56 → 56/149), statut REFONTE-PLAN Phase 22 Argos (📋 PLANNED → 🟢 shippé ADR-0100, src/app/(public)/argos existe depuis v6.25.27). Fix-only, zéro feature. Hors phases 0-9 ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-11 | #453 | feat(seshat) scoring conscient de l'échelle de marché (ADR-0126) | Claude (opérateur Alexandre) | Carte blanche opérateur suite à l'inspection scoring (« notes similaires marque de quartier vs nationale vs internationale ») : 3 champs déclarés additifs sur Strategy (marketScale/addressableAudience/brandFoundedYear, migration backfill-safe) + canon domaine market-scale.ts (cibles d'évidence par échelle, NATION==constantes historiques — continuité Loi 1, densité 5% adressable bornée) + plafond d'évidence scale-aware (fini les 1000 superfans universels, patrimoine = année de fondation déclarée) + fix bug d'unités devotion→cult (3 dimensions saturées à 100 dès ~1% de dévotion, ×100/×200/×300/×500 fraction sur des pourcentages) + exclusion UGC du dénominateur + référentiel affiché sur le KPI score. Hors phases 0-9 mais s'inscrit dans l'impl ADR-0086 (closure-target #15) ; gate out-of-scope : label + justification body + trace ici. 1 migration additive · 0 nouveau modèle · 0 Neter (mesure sous SESHAT) · 0 LLM. 16 tests anti-drift. Cap APOGEE 7/7. |
| 2026-07-11 | #453 | feat(seshat) « tout le reste » du chantier échelle — Overton polity + writer gouverné + surfaces (2ᵉ vague même PR) | Claude (opérateur Alexandre) | Mandat opérateur « shippe tout le reste mais fais ça bien » : ADR-0127 SectorPolityAxis (secteur × échelle × pays, résolution honnête EXACT→SCALE_ONLY→GLOBAL_FALLBACK, Intent SESHAT_UPSERT_POLITY_AXIS requireOperator, radar founder surface axisPolityResolution) + naissance gouvernée SuperfanProfile (SESHAT_REGISTER_SUPERFAN, test HARD single-writer — ferme le vecteur d'inflation d'évidence avant toute ingestion CRM) + MarketScaleCard hub Fondation + classement console à échelle comparable + annotation preUnitsFix + EFR avec échelle gelée. Restes honnêtes : harvesting Tarsis auto par polity (contract-gated), pondération fenêtre (calibration-gated pattern Phase 23). Hors phases 0-9 (impl ADR-0086 target #15) ; gate out-of-scope : label + justification body + trace ici. 1 modèle additif · 2 Intent kinds + SLOs · 0 LLM · 0 bypass. 22 tests. Cap APOGEE 7/7. |
| 2026-07-11 | #454 | docs(governance) drift post-merge PR #453 — compteur Intent kinds 546 → 548 | Claude (opérateur Alexandre) | Phase 9 nefer-postmerge après merge #453 : les 2 kinds ADR-0126/0127 (SESHAT_REGISTER_SUPERFAN, SESHAT_UPSERT_POLITY_AXIS) portent le registre à 548 — NEFER.md §4 et bible couche 6 rafraîchis (recompte daté PM). Version unique 6.27.95 déjà alignée (bump préventif) ; 9.4/9.5 clean. Fix-only 3 fichiers. Hors phases 0-9 ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-11 | #455 | fix(cockpit) sélecteur de marque — fuite cross-tenant + quick intakes + lazy loading + overflow | Claude (opérateur Alexandre) | Bug report opérateur (« sélections multiples, impossible d'ouvrir, dépassement, pas de lazy loading, quick intakes ? ») : inspection → cause racine = brandTreeForSelector non scopé pour un founder USER (BrandNodes de TOUS les tenants exposés — fuite de noms + tuiles cliquables étrangères). Fix : scope nodes miroir du scope stratégie (founder = ses nodes + ancêtres, clôture bornée 7 niveaux) · QUICK_INTAKE exclus du picker/fallback/sélection persistée (résolution = Console, confirmé) · arbre chargé à l'ouverture seulement + erreur affichée (fini le bouton muet) · bouton fluide truncate · dédupe coches multi-nodes. 10 verrous brand-selector-scope.test.ts. Hors phases 0-9 (bugfix sécurité+UX) ; gate out-of-scope : label + justification body + trace ici. 0 migration · 0 modèle. Cap APOGEE 7/7. |
| 2026-07-11 | #456 | fix(cockpit) sélecteur — coches fantômes null===null sur tuiles non pilotées | Claude (opérateur Alexandre) | 2ᵉ passe picker (captures opérateur post-#455) : les ✓ multiples venaient de `tile.strategyId === activeStrategyId` = `null === null` quand aucune marque active → toutes les tuiles « Pas encore piloté » cochées. Garde single-point BrandTile `active = isActive && isPiloted`. Diagnostic complémentaire tracé : la contradiction picker-plein/dashboard-vide des captures = signature de l'ancien code non déployé (fuite cross-tenant réparée v6.27.96). Hors phases 0-9 (bugfix UX) ; gate out-of-scope : label + justification body + trace ici. 0 migration · 0 modèle. Cap APOGEE 7/7. |
| 2026-07-11 | #457 | fix(cockpit) dashboard vide = drift de schéma (marketScale non migrée) + dégradation honnête | Claude (opérateur Alexandre) | Vérification RUNTIME du bug persistant (login réel prod + appel endpoints) : picker-plein/dashboard-vide = `strategy.list` 500 « column Strategy.marketScale does not exist » (migration ADR-0126 dans l'image `c482ab9e` mais jamais appliquée sur la DB Coolify — migrate-on-boot best-effort skippé/en échec). Ni le sélecteur, ni un compte/authz. Code : provider expose `isError`, dashboard affiche une erreur honnête au lieu du faux « Créez votre première marque » qui masquait la panne. Déblocage prod = `npx prisma migrate deploy` (action ops, pas d'accès DB depuis la session). Hors phases 0-9 (bugfix diagnostic+robustesse) ; gate out-of-scope : label + justification body + trace ici. 0 migration nouvelle · 0 modèle · 0 bypass. Cap APOGEE 7/7. |
| 2026-07-11 | #458 | fix(build) migrate-on-boot par runner pg maison (CLI Prisma mort en standalone) | Claude (opérateur Alexandre) | Root cause des 2 incidents de migration (2026-07-10 503, 2026-07-11 dashboard vide) diagnostiquée dans le conteneur prod : le CLI Prisma est inutilisable en image standalone (WASM absent — `.bin/prisma` symlink déréférencé par COPY ; puis deps `@prisma/config`/`effect` 34 Mo élaguées par le trace). Correctif racine : runner maison `scripts/apply-migrations.mjs` ZÉRO-dep (juste `pg`, déjà tracé) qui reproduit `migrate deploy` (pending en ordre, suivi `_prisma_migrations` + checksum sha256 Prisma-compatible vérifié byte-à-byte), appelé par l'entrypoint ; Dockerfile ne copie plus le CLI ni le shim cassé. Vérifié E2E contre Postgres 16 éphémère (60 migrations from-scratch + idempotent + colonnes marketScale/SectorPolityAxis créées). Le drift v6.27.98 se répare au prochain deploy. Hors phases 0-9 (infra build/ops) ; gate out-of-scope : label + justification body + trace ici. 0 migration nouvelle · 0 modèle · 0 bypass. Cap APOGEE 7/7. 7 verrous migrate-on-boot-runner. |
| 2026-07-11 | #459 | ci durcir la gate phase-label (skip opened/reopened) + rétention leçons anti-récurrence | Claude (opérateur Alexandre) | Reproche opérateur (« tu as ce problème de manière systématique, pourquoi tu n'as pas mis à jour ton process ? ») : la gate « Phase label present » rougissait sur CHAQUE PR au run `opened` (l'API create-PR ne pose pas de label) — faux négatif systématique re-diagnostiqué à chaque fois. Corrigé À LA SOURCE (job saute opened/reopened, 1er run = labeled → vert, gate tient). + leçons récurrentes encodées dans skill nefer-ship §7.3/§7.5 (numéro PR créé avant scope-drift, script bump-version.mjs pour la version unique, commit merge GitHub jamais amendé, migrations standalone via runner pg). Hors phases 0-9 (CI + process) ; 0 code applicatif · 0 migration · 0 bump version app. Cap APOGEE 7/7. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #460 | chore(deploy) Coolify seule cible — purge Vercel du repo | Claude (opérateur Alexandre) | Mandat opérateur « c'est déployé entièrement sur Coolify sur un VPS Hostinger donc supprime Vercel de ton processus au passage ». Déploiement 100 % Coolify (build via Dockerfile) : `vercel.json` supprimé (buildCommand Vercel-only, inerte sur Coolify), commentaire `next.config.ts output:standalone` recadré image Docker Coolify, `probe:prod` → `https://powerupgraders.com`, commentaires/exemple `PROD_BASE_URL` de `scheduled-ops.yml` recadrés Coolify. Hors phases 0-9 (infra/config déploiement) ; 0 code applicatif runtime · 0 migration · 0 modèle · 0 bypass. Cap APOGEE 7/7. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #463 | fix(cockpit) crash React #310 (hook après early return) 7 surfaces + garde CI rules-of-hooks | Claude (opérateur Alexandre) | Bug report opérateur (« Une erreur est survenue » sur /cockpit/operate/newsletter). Diagnostic RUNTIME (console prod via Chrome connecté) : React #310 « rendered more hooks than during the previous render » — un useState/useMemo/useCanOperate déclaré APRÈS un early return !strategyId/isLoading (régression lot 14 « fin des dialogues natifs »). Hook hissé au-dessus de tout early return sur 7 surfaces (newsletter, jehuty-feed ×2, notoria, payment-provider-guide, view-mode-selector, guidelines, portfolio [corporateSlug]). Garde durable : le job CI « Lint » exécute eslint.config.mjs (pas next lint) où react-hooks n'était pas enregistré → la classe passait la CI ; plugin react-hooks + rules-of-hooks:error (stories exclues), plugin déjà au lockfile (transitif eslint-config-next). Hors phases 0-9 (bugfix runtime + durcissement CI) ; 0 migration · 0 modèle · 0 bypass. Cap APOGEE 7/7. Gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #464 | feat(newsletter) envoi email réel par marque via Brevo (BrandEmailConnector) | Claude (opérateur Alexandre) | Demande opérateur (« le client spawt veut tester la newsletter, voici ses credentials Brevo, implémente proprement »). `newslettersSend` simulait l'envoi (crmMessage SENT sans email parti) → câblage réel. Modèle `BrandEmailConnector` par Strategy (clé du compte du CLIENT en DB, pattern Vault ADR-0021, jamais en dur/env, jamais renvoyée au client ; distinct de l'email système env ADR-0075) → la clé d'une marque n'envoie QUE ses newsletters (multi-tenant). `sendViaBrevo` réel (POST /v3/smtp/email) + `brand-email.ts` résout ACTIVE→marque, sinon système, sinon DEFERRED explicite (0 succès simulé). 4 procédures tRPC (validation clé EN RÉEL /v3/account + refus sender non vérifié avant activation) + panneau opérateur (masqué fondateurs). List-Unsubscribe RFC 8058. 1 migration additive · 1 modèle · 0 nouveau Neter (email = sous-domaine Anubis, cap 7/7) · 0 bypass. 4 tests. Hors phases 0-9 (feature client) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #466 | feat(cockpit) réseaux de la marque OAuth founder + logo/actifs + veille articles + seed Motion19 | Claude (opérateur Alexandre) | Demande opérateur (« le dashboard oublie le logo/les actifs/la veille ; intégrer un moyen pour que le client connecte ses réseaux lui-même comme Sprout Social ; remplir l'ADVE de Motion19 depuis le web »). Réconciliation ADR-0128 de 5 briques existantes : oauth-integrations (orphelin d'UI) étendu (x PKCE, tiktok client_key, branche ?social=1) → SocialConnection dormant reçoit son 1er écrivain de production (tokens AES-GCM chiffrés AVANT l'émission) ; 3 Intent kinds ANUBIS + sync P22-1 → FollowerSnapshot → suivi communauté existant ; dashboard cockpit complété (logo du coffre, « Mes réseaux » états honnêtes, « Veille & actualités » items[] additif RSS-only) ; seed Motion19 sources publiques (DRAFT + fieldCertainty INFERRED, zéro traction inventée, scale à DÉCLARER). Vagues 2-3 même PR (mandats opérateur successifs) : mission Guilde « Gestion du Digital — Motion19 » 200 000 FCFA/mois + freelance Maximus + candidature ; ADR-0129 StrategyCollaborator (accès délégué par marque via chokepoint canonique, trou pré-existant fermé — calendrier/publications sans garde d'ownership, FREELANCE/CREATOR au cockpit, grant/revoke Intents IMHOTEP) ; ADR-0130 cockpit aux couleurs de la marque (palette du coffre, hex validés, Motion19 #3384FF, E2E vérifié) ; brand book officiel ingéré (canon corrigé, coffre CHROMATIC+TYPOGRAPHY, source OFFICIAL) ; benchmark Sprout/Zoho + plan upgrade S1→S5 (docs/audits/SOCIAL-SUITE-BENCHMARK-2026-07-12.md). Vagues 4-5 (mandats successifs même session) : ADR-0131 zones d'écriture par rôle collaborateur (firewall d'émission 2 voies, veto audité, ferme la sur-délégation ADR-0129 v1, Maximus→SOCIAL_MANAGER) + double dashboard stratégique/« Suivi du jour » (operate/center rebranché, garde lot 12 retirée, pilotage détaillé canOperate) + mode jour cockpit (tokens light existants, toggle persisté) + vitrine campagne/créations + logo sidebar + mini console Guilde /creator. 1 migration additive · 1 modèle · 5 Intent kinds · 0 nouveau Neter (cap 7/7) · 0 bypass. Hors phases 0-9 (feature client cockpit) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #468 | feat(social+cockpit) P1 + vague « cockpit qui ramène tout » (Shopify, Connexions, pages publiques) | Claude (opérateur Alexandre) | Phase P1 du plan d'implémentation explicitement VALIDÉ par l'opérateur (audit de session → « Tout le train P1→P6 ») + mandat suivant même session (« Motion19 a une boutique Shopify… corrige ça aussi ; cockpit qui ramène tout ; page Xtincell »). SocialPost premier écrivain (FB/IG/YT métriques publiques, X/TikTok/LinkedIn UNSUPPORTED honnêtes, upsert clé unique), kind gouverné ANUBIS_SYNC_SOCIAL_POSTS délégable zone social (firewall ADR-0131), cron /api/cron/social-sync (télémétrie, précédent external-feeds), « Suivi du jour » réel (Engagement récent + Meilleures publications), pont pilier E CONNECTOR-first dans ENRICH_E_FROM_PUBLIC_FOOTPRINT (gateway/provenance ADR-0121 inchangés), light-fix OperationsCenter. ADR-0132 : provider shopify par-boutique validé strict, MediaPlatformConnection premier écrivain, hub Connexions, Strategy.publicSlug + /b/[slug] + réécriture proxy sous-domaines, cron ventes. 1 migration additive (publicSlug) · 0 modèle · 3 Intent kinds · 0 nouveau Neter (cap 7/7) · 0 bypass. Gauntlet : tsc 0 · lint 0 · gouvernance 932/932. Hors phases 0-9 (feature client) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #469 | feat(admin) seed-brands déclenchable par bearer CRON_SECRET (ops) | Claude (opérateur Alexandre) | Mandat opérateur « gère tout » (branchement prod des connecteurs OAuth réels via l'API Coolify). Le seed des marques de démo (Motion19 + Xtincell) n'était déclenchable que par session ADMIN (bouton console) ; ajout d'une 2e voie même-privilège bearer CRON_SECRET (fail-closed sinon) pour peupler les données en prod post-déploiement sans navigateur. 0 migration · 0 modèle · 0 Intent kind · 0 nouveau Neter (cap 7/7) · 0 bypass. tsc 0. Hors phases 0-9 (endpoint ops) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #470 | feat(public) page /data-deletion (Meta « User Data Deletion » + RGPD) | Claude (opérateur Alexandre) | Meta/Google/LinkedIn exigent une URL de suppression des données pour valider l'app OAuth (« Your submission is missing data: Suppression des données utilisateur »). Page publique /data-deletion (route group (public), sans auth) : déconnexion self-service immédiate (purge token), révocation plateforme, suppression définitive sur demande 30j. Lien ajouté depuis /privacy. 0 migration · 0 modèle · 0 Intent kind · 0 nouveau Neter (cap 7/7) · 0 bypass. Hors phases 0-9 (page conformité) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #471 | feat(anubis) collecte sociale maximale sous scopes accordés (posts riches + profils publics + provenance) | Claude (opérateur Alexandre) | Mandat opérateur (« l'API permet de collecter plein de données, la fusée devrait tout récupérer dans le cadre de l'alimentation de son système »). Extension directe ADR-0128 (amendée) : posts ×25 avec permalink/visuel/type (3 colonnes additives SocialPost, migration 20260712230000), profil public de marque collecté+rafraîchi (SocialConnection.metadata.profile, FollowerSnapshot.followingCount), pilier E webPresence.connectedProfiles + fix provenance followerSource (CONNECTOR|APIFY réel), frontière PII testée en dur (jamais contenu/identité des tiers, jamais /insights sans scope — protège l'App Review Meta), drift schéma réparé (StrategyCollaborator onDelete: Cascade annoté, pillarServed @default([])). 1 migration additive · 0 modèle · 0 Intent kind (payload étendu non-secret) · 0 nouveau Neter (cap 7/7) · 0 bypass. Gauntlet : tsc 0 · lint 0 · gouvernance 935/935. Hors phases 0-9 (feature client/telemetry) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #471 | feat(anubis) suite sociale pilotable v1 (ADR-0133 — inbox, publication, stats, notifications, CGU) | Claude (opérateur Alexandre) | Mandat opérateur même session (« Je veux les commentaires, les statistiques, les rapports de performances, tout le nécessaire pour piloter, publier, planifier, y compris les notifications — mets à jour les CGU ; ne fais pas mentir le produit »). Scopes de pilotage canon (actifs mode testeurs, App Review = public seulement), SocialInboxItem + services distincts social-inbox/social-publish/social-insights/social-report, 3 Intent kinds ANUBIS + SLOs + zones social, calendrier unique (BrandAction + cron ré-émission spine), 3 surfaces cockpit + nav + i18n ×3, fan-out notifications ADR-0025 réutilisé, CGU §5 mandat de gestion + privacy + data-deletion alignées. + fix Golden Path E2E (canonicalisation domaine épargnant les hôtes locaux — régression v6.27.111) + webhook LinkedIn (challenge HMAC). 1 migration additive (1 modèle + 1 colonne) · 3 Intent kinds · 0 nouveau Neter (cap 7/7) · 0 bypass. Gauntlet : tsc 0 · gouvernance verte · 8 verrous social-suite-pilot. Hors phases 0-9 (produit client) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #472 | feat(cockpit) suggestion d'audience adressable (ADR-0126 amendée — relevés réels, déclaration au porteur) | Claude (opérateur Alexandre) | Mandat opérateur (« Câble la Suggestion »). Doctrine INCHANGÉE (audience adressable = ancre de score DÉCLARÉE, jamais auto-écrite — ADR-0085) : computeAudienceSuggestion (domaine pur, somme + plancher des derniers relevés réels par réseau, null sans donnée), query lecture strategy.getAudienceSuggestion tenant-gardée, pré-remplissage du champ sur la carte Échelle de marché — seul « Enregistrer » du porteur écrit. 0 migration · 0 modèle · 0 Intent kind · 0 nouveau Neter (cap 7/7) · 0 bypass. Gauntlet : tsc 0 · gouvernance 946/946. Hors phases 0-9 (feature client cockpit) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #474 | fix(anubis) dialecte OAuth Meta — config_id Login for Business + purge params Google | Claude (opérateur Alexandre) | Rapport opérateur (« Sorry, something went wrong » post-login Facebook sur le start Meta). Sondage anonyme : scopes valides pré-login → cause côté app Business (« Facebook Login for Business » exige config_id, rejette scope brut). Support META_LOGIN_CONFIG_ID optionnel (posée → config_id, scope retiré ; absente → inchangé) + access_type/prompt réservés à Google (dialecte). 0 migration · 0 modèle · 0 Intent kind · 0 Neter · 0 bypass. 9 tests dialectes/webhook/baseUrl. Hors phases 0-9 (connecteur client) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #476 | ci(ops) build déporté — image construite en CI (ghcr) + boot smoke-test, VPS ne fait que tirer | Claude (opérateur Alexandre — carte blanche) | Cause racine des 3 blackouts de la nuit : next build sur le VPS de prod → OOM. Workflow build-image.yml (runners GitHub, boot smoke-test contre Postgres jetable, push ghcr latest/version/sha) + runbook opérateur docs/deploy/BUILD-DEPORT.md (bascule Coolify source « Docker Image », réversible). Dockerfile/entrypoint/migration au boot INCHANGÉS. NON auto-mergé (le merge peut déclencher un dernier build VPS — à faire hors pic avec la bascule). 0 mutation runtime · 0 migration · 0 modèle · 0 Intent kind · 0 Neter · 0 bypass. Hors phases 0-9 (infra CI/ops) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-12 | #476 | fix(anubis) connexion Meta — l'utilisateur choisit sa Page (auth_type=reauthorize + nom du compte affiché + bandeau guidage) | Claude (opérateur Alexandre — carte blanche) | Bug rapporté (« ça a connecté mon compte perso et pas ma page, je n'ai pas eu le choix »). Cause : le flux connectait tout ce que /me/accounts renvoie sans étape de choix, et la carte n'affichait pas quel compte. Fix 3 points : forceReselect Meta (auth_type=reauthorize impose l'écran de sélection Page), accountName affiché sur la ligne du hub, bandeau de vérification post-connexion Meta + guidage (rattacher la Page à l'espace pro Meta). Reste opérateur : accès de l'app à la Page pro (portefeuille Business + 10 permissions Configuration). 0 migration · 0 modèle · 0 Intent kind · 0 Neter · 0 bypass. tsc 0 · DS+vocab verts · 10 tests dialecte/webhook. Hors phases 0-9 (connecteur client) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-13 | #477 | fix(cockpit) réseaux — afficher TOUTES les Pages connectées (fin du dédoublonnage par plateforme) | Claude (opérateur Alexandre — carte blanche) | Bug rapporté (« ça m'a connecté à Office Inn, pas ma page »). Vraie cause : hub dédoublonnait à une ligne par réseau (service getBrandSocialHubData + composant SocialHubCard) → les autres Pages connectées (dont la bonne) étaient masquées. Fix : une ligne par compte connecté, relevés rattachés par compte, chaque compte avec nom + Débrancher ; bandeau Meta guide vers révocation Facebook si la Page manque. 0 migration · 0 modèle · 0 Intent kind · 0 Neter · 0 bypass. tsc 0 · DS+vocab verts · service 16/16. Hors phases 0-9 (connecteur client) ; gate out-of-scope : label + justification body + trace ici. |
| 2026-07-13 | #478 | feat(cockpit) réseaux — une ligne par réseau + Page de travail choisie (compact + modal) | Claude (opérateur Alexandre — carte blanche) | Demande opérateur (« je ne veux voir que la Page sur laquelle je travaille, une ligne par réseau, modal au clic, l'outil ne compte que ma Page »). Hub compact une ligne/réseau montrant la Page active, modal de choix (radio Page de travail + déconnexion + connecter une autre), kind gouverné ANUBIS_SOCIAL_SET_PRIMARY_ACCOUNT (active=ACTIVE, sœurs=réserve PAUSED), sync/collecte sur ACTIVE uniquement, connexion en réserve par défaut (auto-active si une seule), relevé initial actif-only. État NEEDS_CHOICE honnête, zone social. 0 migration (statut PAUSED réutilisé) · 0 modèle · 1 Intent kind + SLO + zone · 0 Neter · 0 bypass. tsc 0 · lint 0 · gouvernance 946/946 · DS+vocab verts. Hors phases 0-9 (connecteur client) ; gate out-of-scope : label + justification body + trace ici. |
