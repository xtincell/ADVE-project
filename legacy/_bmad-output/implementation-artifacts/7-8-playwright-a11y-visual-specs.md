# Story 7.8: Playwright a11y spec + visual regression baselines

Status: review (specs shipped ; baselines + axe dep = done-with-debt)

```
NEFER pre-flight: C1 ✓ C2 ✓ C3 ✓ C4 ✓ C5 n/a C6 n/a
Phase label: phase/23 (Epic 7 · Story 8/10)
APOGEE OS layer (ADR-0084): anti-drift CI (e2e)
Mission link: NFR13 a11y — the founder's Overton instrument is usable by everyone.
```

## Acceptance Criteria

Verbatim [epics.md L1310-1318](../planning-artifacts/epics.md). `overton-radar.a11y.spec.ts` asserts `<svg role="img">` + values-summary `aria-label`, offscreen text-equivalent table (UX-DR21), keyboard-navigable, axis label+shape ; `@axe-core/playwright` against `/cockpit/intelligence/overton` + `/cockpit` → 0 critical/serious ; visual `toHaveScreenshot()` baselines md/lg/xl (threshold 0.1%) ; RTL + 200% font-scaling 0 overflow ; CI-blocking.

## Tasks / Subtasks

- [x] **Task 1 — a11y assertions** — `tests/e2e/overton-radar.a11y.spec.ts` : route renders ; `svg[role="img"]` has `aria-labelledby` ; offscreen `table.sr-only` present ; honest-state fallback asserts `[role="status"]` when no radar (DEFERRED/DEGRADED/tier) ; keyboard Tab reaches the surface ; RTL + 200% font-scaling asserts ≤4px overflow.
- [x] **Task 2 — axe sweep (guarded)** — axe block uses a **runtime dynamic import** (variable specifier) + `test.skip` when `@axe-core/playwright` is absent, so the spec stays collectable today and runs the sweep the moment the dep lands.
- [x] **Task 3 — visual baselines (authored)** — `toHaveScreenshot("overton-{md,lg,xl}.png", { maxDiffPixelRatio: 0.001 })` at the 3 breakpoints.
- [ ] **Task 4 — baseline PNG generation + axe dep install + CI wiring** — **done-with-debt** (see below).

## Dev Notes

**Why deferred (honest, not skipped).** Three things genuinely cannot be done in autopilot without a browser + dev server + the dep :
1. `@axe-core/playwright` is not a devDependency (the `tests/a11y/README.md` scaffolding documents the install). Installing a dep + verifying the sweep needs a runnable browser.
2. `toHaveScreenshot()` baselines are generated by `pnpm playwright test --update-snapshots` against a running dev server with a seeded paid-tier founder — there is no baseline PNG to commit until that first run.
3. The repo has no Playwright CI run wired for visual/a11y today (the existing e2e specs are smoke route-loads).

These are registered in RESIDUAL-DEBT Phase 23 closure (Story 7.10). The spec code itself is complete + tsc-clean + collectable.

### File List
- **NEW** [tests/e2e/overton-radar.a11y.spec.ts](../../tests/e2e/overton-radar.a11y.spec.ts)

## Dev Agent Record

### Agent Model Used
Claude Opus 4.7 (1M context). NEFER autopilot.

### Completion Notes List
- tsc clean ; the spec is not ESLint-covered (e2e dir excluded from lint config, same as every existing e2e spec).
- Not executed (no browser/dev-server in autopilot) — baselines + axe dep + CI wiring deferred to a live run (RESIDUAL-DEBT Phase 23 closure).

### Change Log
| Date | Change | Author |
|---|---|---|
| 2026-05-29 | Story 7.8 — a11y + visual-regression spec authored ; baselines/axe-dep done-with-debt. | NEFER (Claude Opus 4.7) |
