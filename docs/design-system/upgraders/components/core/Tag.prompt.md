Pill filter chip / category tag. `active` fills it rouge; `onRemove` adds a removable ✕.

```jsx
<Tag active icon={<Megaphone />}>Marketing</Tag>
<Tag>Branding</Tag>
<Tag>Social Media</Tag>
<Tag onRemove={() => {}}>Growth</Tag>
```
