# REBUILD v7 — plan maître

> Mandat opérateur 2026-07-01 (verbatim condensé) : *« carte blanche totale — UI, UX,
> fonctionnalités. Tout ce qui existe posait les bases de ton arrivée. Droit de tout wipe. Ne garde
> que l'exceptionnel, et seulement si nécessaire. N'essaie pas d'honorer notre effort au détriment
> de la performance. »* Décision : [docs/decisions/0001](decisions/0001-rebuild-from-zero.md).
> Ce fichier est le **plan + le board** — une source, pas quatre.

## 1. Le verdict (pourquoi rebuild, pas refonte)

L'ancienne base (v6.27.75) est riche mais son ratio plomberie/valeur est intenable : ~200 modèles
Prisma, 89 routers, 150+ Intent kinds, un bus de gouvernance mythologique (7 Neteru, gates,
manifests) policé par 876 tests de cohérence narrative, 121 ADRs, et un chantier de renommage
(Mestor→Sia…) de ~440 fichiers **purement cosmétique**. La valeur réelle — la méthode, les moteurs
déterministes, le funnel, le pricing Afrique — représente une fraction du code. On ne renomme pas
un échafaudage : on le retire. Le neuf naît avec les bons noms, la refonte-v3.3 devient **caduque**
(ses *intentions* sont natives ici : LATENT, thot→`finance`, plus de Yggdrasil du tout).

## 2. Ce qu'on garde (l'exceptionnel — banque d'organes `legacy/`)

| Organe | Où dans legacy/ | Sort |
|---|---|---|
| **La méthode** ADVE→RTIS, paliers LATENT→ICONE, needsHuman INFERRED/DECLARED | `src/domain/`, variable bible (`src/lib/types/variable-bible.ts`), blueprint | transplanté (WP-001 ✅ amorce) |
| **Moteurs déterministes** : scoring structurel, 14 composers Oracle, calculators coûts/pricing | `src/lib/utils/scoring.ts`, `services/strategy-presentation/`, `services/financial-brain/` | réécrits à l'identique fonctionnel (WP-005/006) |
| **Contenus** : 35 sections Oracle (design éditorial), prompts, copy landing, KB UPgraders | `services/strategy-presentation/types.ts`, `docs/governance/context/` | transplantés |
| **Doctrine pricing** : formule × zone-indices, jamais de grille statique, mobile money, paiement manuel WhatsApp | ADR-0087/0092/0093, `country-registry`, seeds | schéma tranche 1 déjà prêt (`ZoneIndex`, `Payment`) |
| **Identité visuelle** UPgraders DS (corail/or/panda, Clash/Satoshi, bento) | `docs/design-system/upgraders/`, `src/assets/fonts/` | tokens amorcés (WP-002 complète) |
| **Gateway LLM** multi-provider + fallback + structured-output | `services/llm-gateway/` | re-port simplifié (WP-010) |
| **Tables de référence seedées** (pays, coûts d'action, benchmarks) | `prisma/seed-*.ts` | re-seed (WP-009) |
| **Hash-chain d'audit** (le principe, pas le bus) | `IntentEmission` | réduit à `AuditLog` (1 table, ~30 lignes de code) |

## 3. Ce qu'on wipe (sans regret)

Bus Neteru/Intents/gates/manifests + toute la mythologie interne · 4 portails + 9 route groups →
**1 app, 3 espaces** (public / app / admin) par rôles · ~200 modèles → schéma par tranches (16 en
tranche 1) · 121 ADRs + ~40 docs de gouvernance → décisions neuves numérotées à partir de 0001 ·
876 tests anti-drift narratif → tests de comportement · refonte-v3.3 (spec + mon programme
d'exécution ADR-0121, superseded) · husky/commitlint/scope-drift/phase-labels · Storybook,
Chromatic, Cloudflare Containers, PM2, Vercel config (la cible d'infra est **Coolify**, WP-012).

## 4. Architecture cible

```
src/
  app/        (public)/   → landing, intake funnel, pricing, légal
              (app)/      → workspace client : marque, piliers, livrables, campagnes
              (admin)/    → opérations agence : leads, validation paiements, flotte, référentiels
              api/        → routes techniques (webhooks momo, cron)
  domain/     pur TS, zéro IO : pillars, scoring, composers, costing   ← les organes
  server/     services par contexte : identity, brand, deliverables, funnel,
              finance, market, guild, ai (gateway), audit
  components/ primitives UI sur tokens UPgraders (CVA)
prisma/       schéma par tranches, migrations additives
docs/         ce plan + decisions/
legacy/       ancien monde, LECTURE SEULE (aucun import depuis src/)
```

Conventions : noms plats ; toute mutation métier = 1 service + 1 ligne `AuditLog` hash-chaînée ;
Zod aux frontières ; LLM optionnel partout (déterministe d'abord — l'acquis « Fusée
non-dépendante du LLM » est une loi ici) ; secrets en env uniquement.

## 5. Les work packages (une session d'agent chacun, PRs vers la branche de rebuild)

| WP | Contenu | Dépend | Statut |
|---|---|---|---|
| **001** | Fondations : quarantaine legacy/, scaffold Next+TS+Tailwind+Prisma, domaine pillars, schéma tranche 1, CI verte | — | **SHIPPED** (session fondatrice) |
| **002** | DS & shell : fonts Clash/Satoshi, tokens complets, primitives (Button/Card/Input/Badge), layouts des 3 espaces, landing réelle (copy legacy `landingintake`) | 001 | **SHIPPED** (landing + /tarifs + 8 primitives CVA + tokens/fonts ; layouts (app)/(admin) → WP-003) |
| **003** | Identity : auth (credentials + Google), Workspace/Membership, middleware rôles, tRPC si besoin | 001 | **SHIPPED** (credentials+bcrypt+JWT jose, middleware rôles JWT-pur, shells (app)/(admin), AuditLog chaîné + tx, opérateur bootstrap env, vérifié E2E sur Postgres jetable ; Google OAuth = post-launch) |
| **004** | Funnel intake : formulaire public → `IntakeLead` → diagnostic gratuit (scoring déterministe) → CTA conversion → seed workspace+brand+pillars | 002, 003 | **SHIPPED** (wizard /intake 5 étapes piloté par la bible + `funnel.ts` submit/résultat/convert + page résultat jauge /100 + conversion : seed piliers ADVE certainty INFERRED (nom DECLARED), `PillarRevision` v1 chaînée reason intake, `BrandScore` initial, lead CONVERTED ; inscription sans lead intacte ; smoke E2E Postgres réel OK) |
| **005** | Cœur marque : éditeur piliers ADVE (amendement opérateur, needsHuman INFERRED→DECLARED), dérivation RTIS, `PillarRevision` hash-chaînée, `BrandScore` (port scoring 8-dim simplifié) | 003 | **SHIPPED** (moteurs domaine 47 champs/scoring/rtis/oracle + app : bento 8 piliers, éditeur par champ INFERRED→DECLARED, refus RTIS manuel, dérivation RTIS, révisions chaînées re-vérifiées E2E) |
| **006** | Livrables : registre kinds, port des composers déterministes Oracle (sections prioritaires), rendu web + PDF, staleness simple (STALE au rewrite majeur) | 005 | **SHIPPED** (/app/oracle compose explicite + STALE + sections insuffisantes actionnables + /print CSS, markdown-lite XSS-safe, Deliverable upsert audité) |
| **007** | Finance : plans + pricing par formule (zone-indices), souscription mobile money (Wave/OM/MTN/Moov) + **paiement manuel WhatsApp + file de validation admin** (l'acquis pragmatique), gating par plan | 003, 009 | **SHIPPED** (`market.ts` pricing pur lookup ZoneIndex par zone fallback UEMOA, devise dérivée DB, flag placeholder ; `finance.ts` request→approve/reject transactionnels + audités, `Payment` confirmé, échéances 30 j/92 j, réf courte, wa.me pré-rempli env `MANUAL_PAYMENT_WHATSAPP` ; /app/facturation réel + gate Oracle `entitlements.ts` (abo actif OU grâce découverte 15 j) ; **console admin livrée** : /admin compteurs + /admin/leads + /admin/paiements Valider/Rejeter + historique + /admin/marques ; smoke Postgres réel vert. Résidu : momo API providers = post-launch ; CRUD référentiels admin reste ouvert) |
| **008** | Campagnes & missions : Campaign→Action→Brief→Mission (l'essence d'ADR-0119/0120 legacy, simplifiée), coûts d'action par marché | 005 | **SHIPPED** (schéma tranche 2 additive : `Campaign`/`CampaignAction`/`Brief`/`Mission` + 4 enums ; pipeline à gates explicites — cadre DRAFT → « lancer la production » (≥ 1 action) → « transformer en brief » (frame découplé ADR-0120, pré-rempli des données déclarées) → « valider le brief » (objectif+livrable requis, contenu figé) → « éclater en missions » → circuit OPEN→ASSIGNED→DELIVERED→VALIDATED sans saut ni retour ; coûts d'action = `ZoneIndex` famille `action-cost` (12 archétypes portés du catalogue legacy ADR-0093, Σ atomes @ CM) × ratio cost-of-living du marché de la campagne, parité XOF/XAF — trou de référentiel / hors CFA = « à estimer » honnête, jamais de montant inventé ; `server/campaigns.ts` : mutations transactionnelles + `AuditLog` chaîné + flips atomiques + tenancy par marque ; UI : /campagnes (+[id], action/brief/mission) + /missions groupée par étape, DS bento sombre, EmptyStates honnêtes, sidebar groupe Production ; tests purs transitions/gates/scaling + smoke Postgres jetable vert (pipeline complet, chaîne d'audit re-déroulée) ; assignation = nom déclaré, guilde/talents → WP-011) |
| **009** | Référentiels : seeds pays/zones/coûts (port des seeds legacy), admin CRUD référentiels | 003 | **SHIPPED** (seed.mjs : 18 pays + pricing formule réelle 8000 FCFA/mois + cost-of-living ; CRUD admin → WP-007/008) |
| **010** | IA : gateway multi-provider (Anthropic→OpenAI→Ollama→OpenRouter) + structured call Zod + budget/coût par workspace ; branchée sur intake narratif + piliers draft | 004, 005 | **SHIPPED** (`server/ai/` : gateway fetch natif 4 providers par env, timeout 30 s, jamais de throw + `structuredCall` Zod retry×1 + `wrapUntrusted` `<donnees_marque>` + brouillons IA champs VIDES pilier ADVE → certainty INFERRED « à valider », `PillarRevision` reason `ai_draft` chaînée, score recalculé, audit `pillar.ai_draft`/`ai.fail` ; IA optionnelle : zéro env = zéro bouton ; résidu → budget/coût par workspace + intake narratif) |
| **011** | Guilde : mur des missions public, inscription talents, candidatures (essence ADR-0098) | 008 | **SHIPPED** (schéma tranche 3 : `TalentProfile` (+ `dailyRate` tarif indicatif, seul champ ajouté — additif) · `MissionApplication` (unique mission×talent, APPLIED→SHORTLISTED→ACCEPTED/DECLINED sans retour) · `Mission.openToGuild/assigneeTalentId` ; gates pures `domain/guild.ts` + projection ANTI-FUITE du mur (whitelist `toWallMission` testée : ni marque, ni campagne, ni objectif ne sortent — doctrine ADR-0098) ; `server/guild.ts` : mutations transactionnelles + `AuditLog` chaîné (profil create/update dans la chaîne du talent ; `mission.guild.open|close`, `mission.apply` (acteur = talent, chaîne = marque), décisions ; accepter = flip atomique Mission OPEN→ASSIGNED + candidatures sœurs DECLINED + `mission.assign` payload `via: guilde` — historique d'assignation unifié) ; WhatsApp du talent servi UNIQUEMENT après acceptation ; UI : /studio réel (onboarding/édition profil — compétences, pays du référentiel, tarif indicatif en devise du pays, portfolio — mur avec candidature par pitch, mes candidatures + étape du circuit), page mission côté marque (publier/retirer du mur, candidatures, accepter/shortlister/décliner, mise en relation wa.me), /missions badge « Sur le mur » + compte de candidatures, /la-guilde public : compte RÉEL des missions ouvertes (nombre seul, zéro détail public, fallback honnête si DB injoignable) ; assignation directe conservée en fallback hors Guilde ; tests purs (machine d'états, gates, anti-fuite, schémas) + smoke Postgres réel gated `SMOKE_DATABASE_URL` (parcours 2 talents : unicité, tenancy, chaîne d'audit re-déroulée) — vert sur Postgres jetable local ; résidu → paiement mobile money du talent (rails momo), earnings/learn/QC du portail creator legacy) |
| **012** | Déploiement Coolify : Dockerfile, healthcheck, env, migrations au deploy, domaine, staging | 001 | **SHIPPED** — **https://lafusee-v7.76-13-128-23.sslip.io** (projet Coolify `lafusee-v7` : app depuis repo public branche v7 + Postgres dédié `lafusee-v7-db`, db push+seed au boot vérifiés : pricing 8 000 FCFA affiché depuis ZoneIndex ; HTTPS Let's Encrypt, cookies secure OK ; anciens `lafusee`/`lafusee-postgres` intacts). Historique du blocage réseau conservé ci-dessous pour mémoire : — Dockerfile standalone + entrypoint (db push+seed) + `scripts/deploy-coolify.sh` (idempotent, auto-diagnostiquant : projet+Postgres+app repo public+envs+deploy) + workflow `Deploy Coolify`. Constat 2026-07-01 : `76.13.128.23:8000` **injoignable depuis Internet public** (timeout depuis les runners GitHub ; le run n°1 avait obtenu un 422 → instance intermittente ou firewallée). Débloquer = l'un de : (a) rendre l'instance joignable puis relancer le workflow ; (b) depuis une machine qui la voit : `COOLIFY_URL=http://76.13.128.23:8000 COOLIFY_TOKEN=<token régénéré> bash scripts/deploy-coolify.sh` (bash+curl+jq) ; (c) fournir une URL joignable. |
| **013** | **Bascule** : parité funnel validée, backup ancienne base, migration des données vivantes (clients/leads réels uniquement), DNS, merge vers main, wipe ancien projet Coolify | tous | PENDING — **GO opérateur explicite** |
| **014** | Site de marque public : /agence /methode /services /realisations /blog(+6 articles réels du seed legacy, statiques) /contact /la-guilde + nav complète/burger + footer produit-agence-légal + home (preuve sociale réelle STATS/CLIENT_STRIP + bandeau pages) | 002 | **SHIPPED** (copy portée de `legacy/(marketing)/*` + `legacy/src/components/upgraders/data.ts`/`posts.ts` ; paliers/scores affichés = constantes réelles `domain/scoring` ; zéro table, zéro dépendance ; collision de route résolue : l'espace flotte `(app)/agence` concurrent renommé → **/espace-agence** (+ lien /portails mis à jour) ; sitemap enrichi des pages marque + slugs blog) |
| **015** | Console opérateur vague 1 — profondeur /admin sur les tables v7 existantes (esprit des panneaux legacy `(console)` socle/governance/accounts, SANS nouveau modèle) : /admin/utilisateurs (+fiche : memberships/rôles, activité réelle via AuditLog), /admin/workspaces (+fiche : membres, marques, abonnements, paiements, audit), /admin/abonnements (vue cross-workspace, statuts DÉRIVÉS des règles finance.ts, filtres dont « expirent sous 7 j »), /admin/referentiels (CRUD réel Country + ZoneIndex : source obligatoire, validFrom, correction/suppression tracées avant/après — l'opérateur édite les barèmes en base, clôt le résidu « CRUD référentiels » de WP-007/009), /admin/audit (journal filtrable action/chaîne/dates + bouton « Vérifier l'intégrité » : recalcul des selfHash chaîne par chaîne, ruptures localisées HASH_ALTERE/CHAINAGE_ROMPU, tolérance documentée aux fourches concurrentes), accueil /admin 8 compteurs + topbar 8 entrées | 003, 007, 009 | **SHIPPED** (`server/admin.ts` : lectures paginées take/skip + mutations transactionnelles auditées `country.upsert`/`zone_index.create|update|delete` + `verifyChainRows` pur testé (26 tests `tests/admin-console.test.ts`) ; smoke Postgres réel gated `SMOKE_DATABASE_URL` (`tests/admin-smoke.db.test.ts`) : CRUD audité vérifié + altération SQL d'une ligne détectée et localisée pendant que la chaîne sœur reste OK ; pages vérifiées E2E HTTP sur build prod ; zéro touche à prisma/schema.prisma) |
| **016** | Cockpit marque vague 1 — profondeur de l'espace client sur les tables v7 EXISTANTES (port du cluster legacy `(cockpit)/brand/*` + `insights/*`, zéro nouveau modèle) : /app/diagnostic (historique BrandScore liste+delta, breakdown 8 piliers, prochaines actions dérivées des champs manquants via `domain/diagnostic`), /app/rtis (+/synthese : lecture des 4 dérivés, provenance réelle `_derivedFrom`, fraîcheur vs socle, re-dérivation ; synthèse = composition déterministe lisible `domain/rtis-synthese`), 3 vues éditoriales /app/positionnement · /app/proposition · /app/offre (mapping pur thème→champs `domain/pillar-views`, liens d'édition vers les piliers — des vues, zéro donnée), /app/revisions (timeline PillarRevision cross-piliers qui/quand/quoi/pourquoi + chaîne de hash VÉRIFIÉE par recalcul réel des selfHash aux formats des 4 écrivains intake/amend/ai_draft/rtis_refresh), /app/exports (hub Deliverable + staleness lue) ; sidebar groupée | 005, 006 | **SHIPPED** (`server/brand.ts` étendu en lecture seule : `getBrandScores`/`scoreDimensions25`/`getBrandDeliverables`/`getBrandRevisionAudit` + `verifyRevisionChain` pur (statuts ok/unsigned/broken_link/hash_mismatch, chaîne OK/RUPTURE/NON_SIGNEE) ; `domain/` : pillar-views, rtis-synthese, revision-diff — purs, testés (4 suites, 38 tests) ; smoke Postgres réel gated `SMOKE_DATABASE_URL` (`tests/brand-audit-smoke.db.test.ts`) : funnel intake→convert→amend→draft IA→dérivation→Oracle puis falsification SQL d'une révision → RUPTURE détectée et localisée, chaînes sœurs OK ; 8 pages vérifiées E2E HTTP (session réelle, données réelles) sur build prod ; PARITY : 18 lignes brand/insights FUSIONNÉ, 7 laissées À PORTER faute de table (assets/guidelines/notoria/jehuty/benchmarks/attribution/apogee) ; zéro touche à prisma/schema.prisma) |
| **017** | Site public vague 2 — distinction SITE UPgraders vs SITE La Fusée + intake complet : /lafusee (LA vitrine PRODUIT, port de legacy `(marketing)/lafusee` — doctrine superfans×Overton, radar 8 piliers interactif branché sur le vrai moteur, features réellement livrées v7 (diagnostic, cockpit piliers, Oracle, campagnes→missions, guilde), paliers canon, CTA /intake), /landingintake (landing d'acquisition courte port du handoff legacy : copy réelle hero/constat/étapes/protocole, preuve = STATS/CLIENT_STRIP canon, formulaire express → /intake — métriques et témoignages sans source NON repris), /intake/score (référence publique du score /200 sur les constantes canon) + /intake/score/[leadId] (score PARTAGEABLE : relecture par id, méta OG, zéro coordonnée, lien mort → redirect référence ; lien exposé sur /intake/resultat), home : section hiérarchie « produit La Fusée » vs « agence UPgraders », nav publique « La Fusée » + footer + sitemap | 004, 014 | **SHIPPED** (pages statiques sauf score/[leadId] force-dynamic, `cache(getLeadDiagnostic)` partagé metadata/page ; PILLAR_QUESTIONS mutualisé site-data ↔ radar ; zéro table, zéro constante inventée — paliers/bornes/formule = `domain/scoring`, diagnostic /100 = `domain/diagnostic` ; typecheck + 292 tests + prisma validate + build VERTS vérifiés isolément sur HEAD+WP (worktree) — l'arbre de travail portait des fichiers rouges d'une session concurrente vague 2, hors périmètre ; smoke HTML prerendu : copy/nav/radar/sitemap OK) |
| **018** | Espace agence profondeur — port du cluster legacy `(agency)/*` (12 pages) sur les tables v7 EXISTANTES (zéro nouveau modèle) : /espace-agence/clients (+[id] fiche tenancy-gated : marques, campagnes + budgets, missions en cours, derniers paiements), /espace-agence/missions (cross-flotte groupée par étape du circuit + candidatures guilde en attente COMPTÉES depuis MissionApplication), /espace-agence/campagnes (statuts, marchés, budget = total réel des coûts d'actions PAR DEVISE + « à estimer » compté), /espace-agence/revenus (paiements `confirmed` par mois/devise ventilés par client + MRR simple = paiements encaissés des abos actifs normalisés 30 j ; commissions = carte « à venir » dashed, pas de table, zéro chiffre), onglets + compteurs production vivants sur /espace-agence | 007, 008, 014 | **SHIPPED** (`server/agency.ts` étendu LECTURE SEULE sur le MÊME périmètre flotte — workspaces BRAND où l'équipe a une membership, jamais de scan global : pures `monthKey`/`aggregatePaymentsByMonth`/`monthlyAmountOver30Days`/`computeSimpleMrr`/`totalEstimatedByCurrency`/`groupByMissionStatus` + loaders `getFleetClientDetail`/`listFleetMissions`/`listFleetCampaigns`/`getFleetRevenue`/`getFleetPulse` ; doctrine tenue partout : deux devises ne s'additionnent jamais, montant non dérivable → ligne `unresolved` motivée, trous « — »/EmptyState ; 21 tests purs `tests/agency-depth.test.ts` + smoke Postgres réel étendu (`tests/agency-smoke.db.test.ts` : lecture cross-flotte de bout en bout — pipeline campagne→mission + candidature + finance — et workspace hors flotte introuvable même existant) ; 5 pages + refus honnête + not-found tenancy + redirect connexion vérifiés E2E HTTP sur build prod (session JWT réelle, données réelles, montants FCFA/XAF rendus) ; PARITY : 6 lignes agency FUSIONNÉ, 6 laissées À PORTER faute de table (commissions/contracts/intake/knowledge/messages/signals) ; zéro touche à prisma/schema.prisma) |
| **019** | Brand vault — assets & guidelines : schéma tranche 4 ADDITIVE (`BrandAsset` : brandId, kind LOGO/COULEUR/TYPO/DOCUMENT/IMAGE, name, value Json structuré, fileRef String? réservé à l'upload futur, ACTIVE/ARCHIVED, version, createdAt) ; /app/vault (coffre : 5 sections, palette rendue depuis les hex déclarés, EmptyState par section, CRUD server actions) ; /app/guidelines (charte dérivée DÉTERMINISTE : pilier E + coffre) ; hub /app/exports enrichi | 005, 016 | **SHIPPED** (`server/vault.ts` : value Json CANONIQUE par kind — `buildAssetValue` pur (hex canonisé #RRGGBB, champs vides OMIS, liens http(s) seulement), mutations transactionnelles + `AuditLog` chaîné `vault.asset.create|update|archive|restore` (update = version++ à verrou optimiste + avant/après tracés ; archiver = la « suppression », rien ne s'efface ; flips `updateMany` conditionnels), tenancy par (id, brandId) ; `domain/guidelines.ts` composer PUR zéro IO : identité verbale = champs E réellement remplis + certitude DECLARED/INFERRED conservée, identité visuelle = coffre actif (archivé = hors charte), usages = UNIQUEMENT rôles/usages/notes déclarés — chaque section cite sa source réelle ou déclare son manque avec CTA, complétude n/3 ; sidebar Livrables (Coffre, Charte) + cartes sur /app/exports ; PAS de stockage binaire cette vague (résidu honnête : `fileRef` en attente, les liens font foi) ; tests : valeurs pures + composer (déterminisme, zéro invention) + smoke Postgres réel gated `SMOKE_DATABASE_URL` (CRUD audité, tenancy étrangère introuvable, charte depuis l'état réel, chaîne d'audit re-déroulée + selfHash recalculés) ; PARITY : brand/assets + brand/guidelines FUSIONNÉ, brand/identity enrichi du versant visuel) |
| **020** | Académie (cluster creator restant) + console long-tail : /studio/academie (liste modules → module → leçon) porté du legacy `creator/learn/*` + catalogue des 4 cours seedés (`legacy/prisma/seed.ts` §16) ; /admin/talents · /admin/campagnes · /admin/exports + topbar 11 entrées | 006, 008, 011, 015 | **SHIPPED** (`components/academy/content.ts` : contenu pédagogique RÉEL porté — 8 piliers question+description verbatim (orthographe normalisée), 13 canaux drivers, 3 cas d'école complets, plans de leçons du seed ; doctrine « rien d'inventé » sur le contenu : leçon sans corps legacy (quiz/vidéos/exercices, cult-marketing, production-creative) = « à migrer »/« contenu en cours de migration », jamais rédigée à sa place ; barème du module fondamentaux RENDU depuis `domain/scoring` (poids 15/7/3, 6 paliers bornes canon — l'échelle 5 paliers de la page legacy est caduque) ; progression = localStorage par appareil, annoncé en clair dans l'UI (`progress.tsx` — RÉSIDU : pas de table Enrollment cette vague, le suivi par compte viendra avec sa table) ; console : /admin/talents (TalentProfile cross-flotte réels + file MissionApplication filtrable — lecture seule, la décision reste à la marque), /admin/campagnes (campagne→actions→missions par étape, budget PAR devise + « à estimer » compté, filtres statut), /admin/exports (Deliverable cross-flotte, fraîcheur RECALCULÉE à la lecture, sections insuffisantes, couverture marques), pagination take/skip réutilisée (`parsePage`/`Pager` WP-015) ; tests contenu `tests/academy-content.test.ts` (11) ; smoke E2E HTTP sur build prod + Postgres jetable : 8 pages académie (dont barème 0–40/…/>180, à migrer, 404) + 3 pages admin (périmé recalculé, à estimer, filtres vides) + gardes (talent → /admin refusé, anonyme → /connexion) VERTS ; typecheck · 369 tests · build · db:validate verts ; zéro touche à prisma/schema.prisma ni au shell (app) — lien sidebar studio→académie laissé au propriétaire du shell (1 ligne) ; PARITY : 24 lignes academy/learn/console statuées) |
| **021** | Triage PARITY : statuer les ~184 lignes restées « À PORTER » — OBSOLÈTE justifié ligne par ligne / À PORTER re-priorisé P1-P3 avec la fonction réelle / FUSIONNÉ vérifié contre src/app (docs-only : PARITY.md + ce board, zéro code) | 014–020 | **SHIPPED** (253 lignes re-statuées, zéro perte : PORTÉ 32 · FUSIONNÉ 85 · OBSOLÈTE 57 — bus Intents/Neteru (hubs mestor/anubis/artemis, model-policy, oracle-incidents, canon-sync), Glory/séquences (skill-tree, tools, vault, sequences, oracle-catalog, calendar), Brand Tree + wizards Phase 18, error-vault/telemetry interne, 17 stubs de redirection (fusee/signal/academie), multi-opérateurs licenciés, dépôt public de missions (anti-fuite ADR-0098) · À PORTER 79 priorisés P1 8 / P2 28 / P3 43 ; section « Synthèse triage 2026-07-02 » ajoutée à PARITY.md (comptes + top-10) ; concurrence respectée : 19 lignes flippées par WP-020 pendant le triage laissées telles quelles, forgot/reset-password re-statuées PORTÉ par WP-022 (/mot-de-passe-oublie, /reinitialiser/[token]) et cockpit/settings fusionnée vers /reglages (WP-022) ; P1 restants (ordre) : partage public Oracle (shared/strategy), rapport diagnostic complet web+PDF (intake result), commissions talents (boucle momo), suivi superfans + relevés sociaux, état Overton, intake ingest IA, benchmarks sectoriels) |
| **022** | AUTH complet + réglages compte : /mot-de-passe-oublie + /reinitialiser/[token] (port de legacy `(auth)/forgot-password`+`reset-password`) — schéma tranche 5 ADDITIVE `PasswordResetToken` (tokenHash unique, expiresAt, usedAt) ; relais opérateur dans /admin/utilisateurs ; /reglages (nom, email, mot de passe, espaces & rôles, session) | 003, 015 | **SHIPPED** (`server/identity.ts` étendu : règles PURES du token — 32 octets d'aléa base64url, SHA-256 SEUL en base (le stockage legacy en clair sur User n'est pas reconduit), TTL 1 h `RESET_TOKEN_TTL_MS`, usage unique `resetTokenState` (USED prime EXPIRED) — + cycle complet transactionnel audité chaîne SYSTÈME `user.password_reset.request|link_issue|complete` (jamais de token/hash dans les payloads), consommation ATOMIQUE `updateMany` anti-course ; SANS provider email (réalité v7) le comportement est HONNÊTE : demande anti-énumération (réponse identique, compte inconnu silencieux) → « un lien a été généré, contactez l'opérateur WhatsApp » (CTA wa.me env `MANUAL_PAYMENT_WHATSAPP`) → file « Réinitialisations en attente » sur /admin/utilisateurs où l'opérateur ÉMET le lien (rotation du hash : le clair ne s'affiche qu'UNE fois, ré-émission invalide l'ancien, demande consommée refusée) — l'acquis validation manuelle appliqué à l'auth ; OUTBOUND_EMAIL env-gated = TODO résiduel documenté, AUCUN fake envoi ; /reinitialiser/[token] : états valide/inutilisable/succès, bcrypt 12, noindex ; /reglages (hors matcher middleware — session vérifiée page + CHAQUE action) : nom, email re-vérifié par mot de passe (unicité, avant/après audités chaîne workspace), mot de passe (8 min), espaces & rôles réels (membership courante marquée), section Sessions avec déconnexion de l'appareil + RÉSIDU affiché en clair : pas de « déconnexion partout » ni d'invalidation des sessions ouvertes (JWT stateless 30 j, pas de rotation de version de session — le poser = tranche future User.sessionVersion + claim JWT) ; tests : 16 purs `tests/identity.test.ts` (aléa/hash/TTL/usage-unique/frontières/URL) + smoke Postgres réel gated `SMOKE_DATABASE_URL` `tests/identity-smoke.db.test.ts` (cycle demande→file→émission×2 (rotation vérifiée : ancien lien mort)→reset→rejeu refusé + réglages + audit) VERT sur Postgres jetable local ; 9 checks E2E HTTP sur build prod (formulaires, confirmation honnête wa.me, token valide/bidon/succès, gardes /reglages et /admin, file opérateur avec email réel) ; typecheck · 385 tests · db:validate · build verts ; résidu UI 2 lignes laissées aux propriétaires des shells : lien « Mot de passe oublié ? » sur /connexion + entrée « Réglages » dans la sidebar (app) ; PARITY : 4 lignes (auth) statuées + //cockpit/settings enrichi) |
| **023** | Circulation des livrables — les 2 P1 de tête du triage : **partage public de l'Oracle** /partage/oracle/[token] (port de //shared/strategy/[token]) + **rapport ADVE complet du funnel** /intake/rapport/[token] (port de //intake/[token]/result), tokens signés STATELESS (zéro migration) | 004, 006, 021 | **SHIPPED** (`server/share.ts` : couche token PURE jose HS256 AUTH_SECRET (même clé que la session, cloisonnée par claim `scope` Zod : `share.oracle` {deliverableId, brandId} / `share.rapport` {leadId}, TTL 30 j, null sur tout token douteux) + résolveurs DB (Deliverable vérifié kind+brandId+contenu parsable ; rapport → `getLeadReport`) ; **Oracle** : bouton « Partager » sur /app/oracle (server action → `createOracleShareLink`, `AuditLog deliverable.share` à CHAQUE génération, jamais le token en payload ; panneau URL absolue résolue navigateur + copie) → page publique read-only (bandeau « Oracle partagé par <marque> · généré avec La Fusée », registre document blanc du /print, noindex, CTA /intake) ; limite V1 DITE dans l'UI : stateless ⇒ lien NON révocable pendant 30 j (révoquer = nonce persistable, tranche future) ; le lien montre la dernière composition (upsert) ; pas de gate d'abonnement (lire un Oracle composé est libre, seule la composition est payante) ; **rapport** : `funnel.ts` étendu LECTURE SEULE `getLeadReport` (constat champ par champ depuis la bible : déclaré (réponse rendue) vs vide (description), diagnostic recalculé jamais stocké) → /intake/rapport/[token] web + imprimable MÊME page (pattern print oracle : @page, break-inside-avoid, chrome public masqué — la page n'utilise aucun header/footer/nav interne) : score /100 + palier, 4 barèmes pilier, forces/angles morts factuels, 47 champs A→D→V→E, 3 actions, méthode expliquée depuis les constantes canon (15/7/3, bornes /200, « vous êtes ici »), CTA conversion état-du-lead ; lien « Voir le rapport complet » sur /intake/resultat (signature pure au rendu, zéro écriture) ; les deux liens morts → pages mortes propres noindex ; tests : 14 purs `tests/share.test.ts` (aller-retour, exp, tampering, secret, claims incomplets, cloisonnement scopes session/oracle/rapport) + smoke Postgres réel gated `SMOKE_DATABASE_URL` `tests/share-smoke.db.test.ts` (funnel→rapport, conversion→Oracle→lien audité→résolution publique, falsifié/inexistant → null) VERT sur Postgres jetable local ; E2E HTTP sur build prod : rapport valide (constat/actions/méthode/answers rendus, converted + non-converted CTA), oracle partagé (bandeau, doc, noindex), tokens falsifiés + cross-scope → pages mortes, bouton Partager rendu sur /app/oracle en session réelle ; typecheck · 399 tests · db:validate · build verts ; zéro touche à prisma/schema.prisma ; PARITY : 2 lignes P1 → PORTÉ + note top-10) |
| **024** | Commissions talents (P1 n°3 du triage — **boucler guilde→mobile money**) : schéma tranche 5 ADDITIVE `TalentPayout` (missionId UNIQUE, talentId, workspaceId payeur, brut/taux/commission/net FIGÉS, devise, PENDING→APPROVED→PAID/REJECTED, method momo/manual, reference) + référentiel taux ; ordre créé à la validation de mission ; /studio « Mes gains » ; /admin/commissions ; carte commissions réelle /espace-agence/revenus | 007, 008, 011, 015, 018 | **SHIPPED** (`domain/payout.ts` PUR : jours facturés ceil(assignation→livraison) min 1, ventilation `round(brut × taux)` avec INVARIANT net + commission = brut testé sur balayage, garde int32, `normalizeCommissionRate` (valeur hors [0, 1) = trou de référentiel, jamais appliquée), machine d'états sans saut ni retour, `summarizePayouts` PAR DEVISE (XOF/XAF jamais additionnées) ; taux JAMAIS en dur : `ZoneIndex` famille "commission" clé `guild.rate` scope GLOBAL — seed additif 0.15 source `placeholder-operator-to-confirm` (AUCUN taux plat en legacy : commission-engine 25–40 % par TIER talent, Hub-Escrow canon 20 %→8 % — taux unique à confirmer, éditable /admin/referentiels, badge « à confirmer » surfacé partout via `isPlaceholderSource`) ; `server/payouts.ts` + `campaigns.validateMission` étendu : l'ordre naît DANS la transaction du flip DELIVERED→VALIDATED (brut = montant saisi par la marque au formulaire de validation, sinon dailyRate × jours dérivé, sinon refus honnête `AMOUNT_REQUIRED` ; référentiel manquant = validation Guilde REFUSÉE avec marche à suivre ; mission hors Guilde = validation sans ordre, dit en clair ; devise = pays du profil talent) ; circuit opérateur /admin/commissions (topbar 12 entrées) : Approuver/Écarter puis « Marquer payé » momo/manual + référence OBLIGATOIRE (pattern /admin/paiements), files ordonnées + totaux PAR DEVISE + taux en vigueur affiché, flips atomiques `updateMany`, audit `payout.create|approve|reject|pay` chaîné dans le workspace payeur ; /studio « Mes gains » réel (net/brut/commission par mission, réf momo une fois payé, « paiement manuel sous 72 h ouvrées », EmptyState honnête) ; /espace-agence/revenus : carte dashed → section réelle (ordres par statut, commissions générées/encaissées, net talents dû PAR DEVISE, même périmètre flotte) ; récap gain sur la page mission validée ; tests : 21 purs `tests/payouts.test.ts` (arrondis, invariant, taux référentiel, machine d'états, devises, frontières Zod) + smoke Postgres réel gated `SMOKE_DATABASE_URL` `tests/payouts-smoke.db.test.ts` (4 parcours : dérivation tarif×jours auditée, montant déclaré qui prime + tarif absent refusé puis accepté, trou de référentiel bloquant + hors-Guilde sans ordre + restauration, circuit opérateur complet avec chaîne d'audit re-déroulée) VERT sur Postgres jetable local ; 6 checks E2E HTTP sur build prod (gardes anonyme/talent, console opérateur avec files/taux/rail momo réels, « Mes gains » avec réf payée, récap mission « brut 100 000 FCFA · commission 15 % = 15 000 FCFA · net 85 000 FCFA », carte flotte) ; typecheck · 420 tests · db:validate · build VERTS ; PARITY : //console/socle/commissions → PORTÉ + //agency/commissions et //creator/earnings/{history,missions} → FUSIONNÉ + notes invoices/revenue) |

Parallélisme : 002∥003∥009 après 001 · 004∥005 après leurs deps · 006/007/008/010 ensuite ·
011/012 quand prêts. Chaque WP : claim ici (colonne Statut → IN_PROGRESS + PR), CI verte, board mis
à jour dans la même PR.

## 6. Règles flotte (condensées — le détail vit dans CLAUDE.md)

Une session = un WP = une PR · porter = réécrire en lisant legacy/ (jamais d'import depuis
`legacy/`) · CI verte à chaque PR (typecheck·test·db:validate·build) · schéma additive-only ·
mutation métier ⇒ AuditLog · données inventées interdites · secrets en env · prod = WP-012/013
uniquement, backup d'abord.

## 7. Questions ouvertes (opérateur)

1. **URL de l'instance Coolify** + token **régénéré** (celui partagé en chat est à révoquer),
   fournis en variables d'env de session — requis pour WP-012.
2. Domaine cible (lafusee.com ? argos.lafusee.com plus tard ?).
3. Données vivantes à migrer à la bascule (liste des workspaces/clients réels à préserver).
4. GO explicite pour WP-013 (bascule + wipe ancien projet Coolify, post-backup).

---
*Créé 2026-07-01 — session fondatrice v7. Ce fichier est le board : le tenir à jour à chaque WP.*
