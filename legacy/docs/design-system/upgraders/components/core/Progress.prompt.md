Progress / objective bar. `tone="level"` gives the gold→ember XP gradient.

```jsx
<Progress value={70} label="Progression générale" showValue />
<Progress value={45} tone="warning" label="Objectif mensuel" showValue />
<Progress value={71} tone="level" size="lg" label="XP 850 / 1200" />
```
